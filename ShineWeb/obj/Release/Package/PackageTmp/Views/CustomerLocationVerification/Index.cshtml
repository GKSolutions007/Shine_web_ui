
@{
    ViewBag.Title = "Customer Location Verification";
    Layout = "~/Views/Shared/_LayoutBT5.cshtml";
}

<link href="~/Content/5.3.8/bootstrap.min.css" rel="stylesheet" />
<link href="~/Content/navbar.css" rel="stylesheet" />
<link rel="stylesheet" href="~/Content/jquery-ui.css">
<link href="~/Content/dataTables.dataTables.2.2.2.css" rel="stylesheet" />
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">

<script src="~/Scripts/5.3.8/bootstrap.min.js"></script>
<script src="~/Scripts/jquery-3.7.1.js"></script>
<script src="~/Scripts/jquery-ui.js"></script>
<script src="~/Scripts/Validation.js"></script>
<script src="~/Scripts/jquery-ui.js"></script>
<script src="~/Scripts/datatable.js"></script>
<script src="~/Scripts/dataTables.2.2.2.js"></script>
<script src="~/Scripts/js/refreshtoken.js"></script>


<style>
    :root {
        --mda-primary: #4361ee;
        --mda-primary-dark: #3145c4;
        --mda-success: #2ec4b6;
        --mda-warning: #ff9f1c;
        --mda-danger: #e5383b;
        --mda-bg: #f4f6fb;
        --mda-card-radius: 14px;
    }

    #dvMainTable {
        animation: mdaFadeIn .45s ease both;
    }

    @@keyframes mdaFadeIn {
        from {
            opacity: 0;
            transform: translateY(10px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    @@keyframes mdaPop {
        0% {
            transform: scale(.85);
            opacity: 0;
        }

        60% {
            transform: scale(1.05);
            opacity: 1;
        }

        100% {
            transform: scale(1);
        }
    }

    @@keyframes mdaPulseIcon {
        0%, 100% {
            transform: scale(1);
        }

        50% {
            transform: scale(1.12);
        }
    }

    @@keyframes mdaShake {
        10%, 90% {
            transform: translateX(-1px);
        }

        20%, 80% {
            transform: translateX(2px);
        }

        30%, 50%, 70% {
            transform: translateX(-4px);
        }

        40%, 60% {
            transform: translateX(4px);
        }
    }

    @@keyframes mdaRowIn {
        from {
            opacity: 0;
            transform: translateY(6px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    /* ---------- Header ---------- */
    .mda-top-row {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 16px;
        flex-wrap: wrap;
    }

    .mda-header {
        display: flex;
        align-items: center;
        gap: 10px;
        padding-top: 6px;
    }

        .mda-header .mda-header-icon {
            width: 34px;
            height: 34px;
            border-radius: 10px;
            background: linear-gradient(135deg, var(--mda-primary), var(--mda-primary-dark));
            color: #fff;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 16px;
            box-shadow: 0 4px 10px rgba(67, 97, 238, .3);
        }

        .mda-header h4 {
            font-size: 16px !important;
            font-weight: 600;
            margin: 0;
            color: #2b2d42;
            letter-spacing: .2px;
        }

    /* ---------- Stat cards ---------- */
    .mda-stats-row {
        display: flex;
        align-items: stretch;
        flex-wrap: wrap;
        justify-content: flex-end;
        gap: 10px;
        margin-top: 10px;
    }

    @@media (max-width: 900px) {
        .mda-stats-row {
            justify-content: flex-start;
            width: 100%;
        }
    }

    @@media (max-width: 480px) {
        .mda-stats-row {
            flex-direction: column;
            width: 100%;
        }
    }

    .mda-stat-card {
        position: relative;
        background: #fff;
        border-radius: 10px;
        padding: 7px 12px;
        width: 125px;
        display: flex;
        align-items: center;
        gap: 8px;
        box-shadow: 0 2px 8px rgba(20, 25, 60, .06);
        border: 1px solid #eef0f7;
        overflow: hidden;
        animation: mdaPop .4s ease both;
        transition: transform .2s ease, box-shadow .2s ease;
    }

        .mda-stat-card:nth-child(1) {
            animation-delay: .02s;
        }

        .mda-stat-card:nth-child(2) {
            animation-delay: .08s;
        }

        .mda-stat-card:nth-child(3) {
            animation-delay: .14s;
        }

        .mda-stat-card:nth-child(4) {
            animation-delay: .2s;
        }

        .mda-stat-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 16px rgba(20, 25, 60, .12);
        }

        .mda-stat-card::before {
            content: "";
            position: absolute;
            inset: 0;
            opacity: .05;
            background: var(--mda-card-accent, var(--mda-primary));
        }

    .mda-stat-icon {
        flex: 0 0 auto;
        width: 28px;
        height: 28px;
        border-radius: 8px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 13px;
        color: #fff;
        background: var(--mda-card-accent, var(--mda-primary));
        transition: transform .25s ease;
    }

    .mda-stat-card:hover .mda-stat-icon {
        animation: mdaPulseIcon .6s ease;
    }

    .mda-stat-body {
        display: flex;
        flex-direction: column;
        z-index: 1;
    }

    .mda-stat-label {
        font-size: 9.5px;
        text-transform: uppercase;
        letter-spacing: .5px;
        color: #8a8fa3;
        font-weight: 600;
        line-height: 1.2;
    }

    .mda-stat-value {
        font-size: 15px;
        font-weight: 700;
        color: #23263a;
        line-height: 1.15;
    }

    .mda-card-limit {
        --mda-card-accent: #4361ee;
    }

    .mda-card-total {
        --mda-card-accent: #7209b7;
    }

    .mda-card-active {
        --mda-card-accent: #2ec4b6;
    }

    .mda-card-deactive {
        --mda-card-accent: #e5383b;
    }

    .mda-card-open {
        --mda-card-accent: #ff9f1c;
    }

    /* ---------- Table ---------- */

    #tblCustomerLocation_wrapper {
        animation: mdaFadeIn .5s ease .1s both;
    }

    table.dataTable.table-bordered {
        border-radius: 10px;
        overflow: hidden;
        border-collapse: separate !important;
        border-spacing: 0;
    }

    /* Header layout: sticky, bolder, spaced-out caps */
    .trclrforcolheader th {
        background: linear-gradient(135deg, var(--mda-primary), var(--mda-primary-dark));
        color: #fff;
        border-bottom: none !important;
        font-size: 12px !important;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: .5px;
        padding: 5px 10px !important;
        white-space: nowrap;
        position: sticky;
        top: 0;
        z-index: 2;
    }

    /* Table body font size */
    #tblCustomerLocation {
        font-size: 14px;
        border-collapse: collapse !important;
    }

        #tblCustomerLocation tbody td {
            /* padding: 10px 10px !important;*/
            vertical-align: middle;
            font-size: 13px;
            border: 1px solid #e3e6f0 !important;
        }

        #tblCustomerLocation thead th {
            border: 1px solid #3145c4 !important;
        }

        #tblCustomerLocation tbody tr {
            animation: mdaRowIn .3s ease both;
            transition: background-color .15s ease;
        }

            /* Zebra striping for odd / even rows.
               DataTables adds .odd / .even classes to every row it renders (this
               survives paging/scrolling/redraws), so we target those directly in
               addition to :nth-child as a fallback. !important guards against
               Bootstrap's per-cell "table > :not(caption) > * > *" background rule
               and the bundled DataTables stylesheet both outranking a plain rule. */
            #tblCustomerLocation tbody tr.odd,
            #tblCustomerLocation tbody tr:nth-child(odd) {
                background-color: #fcecec !important;
            }

                #tblCustomerLocation tbody tr.odd > td,
                #tblCustomerLocation tbody tr:nth-child(odd) > td {
                    background-color: #fcecec !important;
                }

            #tblCustomerLocation tbody tr.even,
            #tblCustomerLocation tbody tr:nth-child(even) {
                background-color: #f4f6fb !important;
            }

                #tblCustomerLocation tbody tr.even > td,
                #tblCustomerLocation tbody tr:nth-child(even) > td {
                    background-color: #f4f6fb !important;
                }

            #tblCustomerLocation tbody tr:hover,
            #tblCustomerLocation tbody tr:hover > td {
                background-color: #dfe4ff !important;
            }

    /* ---------- Status info icon / tooltip ---------- */
    .mda-status-tooltip-wrap {
        position: relative;
        display: inline-flex;
        align-items: center;
        line-height: 1;
        cursor: help;
    }

    .mda-status-info {
        font-size: 13px;
        color: inherit;
        opacity: .8;
        transition: opacity .2s ease, transform .2s ease;
    }

    .mda-status-tooltip-wrap:hover .mda-status-info {
        opacity: 1;
        transform: scale(1.2) rotate(8deg);
    }

    .mda-tooltip {
        position: absolute;
        bottom: calc(100% + 11px);
        left: 50%;
        min-width: 190px;
        background: linear-gradient(135deg, #2b2d42, #23263a);
        color: #fff;
        padding: 10px 13px;
        border-radius: 10px;
        font-size: 11.5px;
        font-weight: 500;
        text-transform: none;
        letter-spacing: normal;
        text-align: left;
        box-shadow: 0 12px 28px rgba(20, 25, 60, .28);
        opacity: 0;
        visibility: hidden;
        pointer-events: none;
        transform: translate(-50%, 6px) scale(.9);
        transform-origin: bottom center;
        transition: opacity .2s ease, transform .2s cubic-bezier(.34, 1.56, .64, 1), visibility .2s;
        z-index: 30;
    }

        .mda-tooltip::after {
            content: "";
            position: absolute;
            top: 100%;
            left: 50%;
            transform: translateX(-50%);
            border: 6px solid transparent;
            border-top-color: #23263a;
        }

    .mda-status-tooltip-wrap:hover .mda-tooltip {
        opacity: 1;
        visibility: visible;
        transform: translate(-50%, 0) scale(1);
    }

    .mda-tooltip-row {
        display: flex;
        align-items: center;
        gap: 7px;
        line-height: 1.6;
        white-space: nowrap;
    }

        .mda-tooltip-row + .mda-tooltip-row {
            margin-top: 3px;
        }

        .mda-tooltip-row i {
            font-size: 11px;
            color: #6fe3d4;
            width: 12px;
            text-align: center;
        }

    /* ---------- Image gallery popup ---------- */
    .mda-image-gallery {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
        gap: 20px;
        text-align: left;
    }

    .mda-image-card {
        border: 1px solid #eef0f7;
        border-radius: 12px;
        overflow: hidden;
        background: #fff;
        box-shadow: 0 2px 8px rgba(20, 25, 60, .06);
        transition: box-shadow .2s ease;
        animation: mdaPop .3s ease both;
    }

        .mda-image-card:hover {
            box-shadow: 0 8px 18px rgba(20, 25, 60, .16);
        }

    .mda-image-zoom-wrap {
        overflow: hidden;
        height: 260px;
        display: flex;
        align-items: center;
        justify-content: center;
        background: #f4f6fb;
        position: relative;
    }

    /* The pre-existing ".mda-image-modal .modal-body img" rule applies the
           mdaPop entrance animation with fill-mode "both", which keeps forcing
           transform back to its own end-keyframe value and silently blocks any
           hover transform set elsewhere. Explicitly cancel it here so the zoom
           transition below actually takes effect. */
    .mda-image-modal .modal-body .mda-image-zoom-wrap img {
        max-width: 100%;
        max-height: 100%;
        width: auto;
        height: auto;
        animation: none !important;
        box-shadow: none;
        border-radius: 0;
        transform: scale(1);
        transform-origin: center center;
        transition: transform .35s ease;
        cursor: zoom-in;
        will-change: transform;
    }

    .mda-image-zoom-wrap:hover img {
        transform: scale(1.9);
    }

    .mda-image-meta {
        padding: 10px 12px;
    }

    .mda-image-name {
        font-size: 13px;
        font-weight: 600;
        color: #23263a;
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
    }

    .mda-image-size {
        font-size: 11.5px;
        color: #8a8fa3;
        margin-top: 2px;
    }

    /* ---------- Responsive ---------- */
    @@media (max-width: 768px) {
        .mda-header h4 {
            font-size: 15px !important;
        }

        #tblCustomerLocation, .trclrforcolheader th {
            font-size: 12.5px !important;
        }

            #tblCustomerLocation tbody td {
                font-size: 12.5px;
                padding: 8px 6px !important;
            }

        .mda-btn-icon {
            font-size: 11px;
            padding: 4px 8px;
        }
    }

    @@media (max-width: 480px) {
        .mda-header h4 {
            font-size: 14px !important;
        }

        .mda-header-icon {
            width: 28px;
            height: 28px;
            font-size: 14px;
        }

        #tblCustomerLocation, .trclrforcolheader th {
            font-size: 11.5px !important;
        }

            #tblCustomerLocation tbody td {
                font-size: 11.5px;
                padding: 6px 5px !important;
            }

        .mda-btn-icon {
            font-size: 10px;
            padding: 3px 6px;
            gap: 3px;
        }
    }

    .mda-btn-icon {
        border: none;
        border-radius: 8px;
        padding: 5px 12px;
        font-size: 12px;
        font-weight: 600;
        display: inline-flex;
        align-items: center;
        gap: 6px;
        color: #fff;
        transition: transform .15s ease, box-shadow .15s ease, opacity .15s ease;
    }

        .mda-btn-icon:hover:not(:disabled) {
            transform: translateY(-1px);
            box-shadow: 0 4px 10px rgba(0,0,0,.15);
        }

        .mda-btn-icon:disabled {
            opacity: .55;
            cursor: not-allowed;
        }

    .mda-btn-activate {
        background: linear-gradient(135deg, #2ec4b6, #21a99d);
    }

    .mda-btn-deactivate {
        background: linear-gradient(135deg, #e5383b, #c9262a);
    }

    /* ---------- Error popup modal ---------- */
    .mda-error-modal .modal-content {
        border: none;
        border-radius: 16px;
        overflow: hidden;
        animation: mdaShake .45s ease .1s both;
    }

    .mda-error-modal .modal-body {
        text-align: center;
        padding: 32px 26px 26px;
    }

    .mda-error-icon-wrap {
        width: 72px;
        height: 72px;
        margin: 0 auto 16px;
        border-radius: 50%;
        background: rgba(229, 56, 59, .12);
        display: flex;
        align-items: center;
        justify-content: center;
        animation: mdaPop .4s ease both;
    }

        .mda-error-icon-wrap i {
            font-size: 32px;
            color: var(--mda-danger);
        }

    .mda-error-title {
        font-size: 17px;
        font-weight: 700;
        color: #23263a;
        margin-bottom: 6px;
    }

    .mda-error-message {
        font-size: 13.5px;
        color: #6c7086;
        margin-bottom: 0;
    }

    .mda-error-modal .modal-footer {
        border-top: none;
        justify-content: center;
        padding-bottom: 26px;
    }

    .mda-error-modal .btn-close-error {
        background: linear-gradient(135deg, var(--mda-danger), #c9262a);
        color: #fff;
        border: none;
        border-radius: 8px;
        padding: 8px 26px;
        font-weight: 600;
        font-size: 13px;
        transition: transform .15s ease, box-shadow .15s ease;
    }

        .mda-error-modal .btn-close-error:hover {
            transform: translateY(-1px);
            box-shadow: 0 6px 14px rgba(229, 56, 59, .35);
        }

    /* ---------- Location / Image link buttons ---------- */
    .mda-link-btn {
        border: none;
        background: transparent;
        display: inline-flex;
        align-items: center;
        gap: 6px;
        font-weight: 600;
        font-size: 12.5px;
        padding: 5px 10px;
        border-radius: 8px;
        cursor: pointer;
        text-decoration: none;
        transition: transform .15s ease, background-color .15s ease, box-shadow .15s ease;
    }

        .mda-link-btn:hover {
            transform: translateY(-1px);
            text-decoration: none;
        }

    .mda-link-location {
        color: #1c8f84;
        background: rgba(46, 196, 182, .12);
    }

        .mda-link-location:hover {
            background: rgba(46, 196, 182, .22);
            box-shadow: 0 4px 10px rgba(46, 196, 182, .25);
        }

    .mda-link-image {
        color: #3145c4;
        background: rgba(67, 97, 238, .1);
    }

        .mda-link-image:hover {
            background: rgba(67, 97, 238, .2);
            box-shadow: 0 4px 10px rgba(67, 97, 238, .25);
        }

    .mda-empty-cell {
        display: inline-flex;
        align-items: center;
        gap: 6px;
        color: #adb0c0;
        font-size: 12.5px;
        font-style: italic;
    }

    /* ---------- Status badges ---------- */
    .mda-status-badge {
        display: inline-flex;
        align-items: center;
        gap: 6px;
        padding: 5px 8px;
        border-radius: 20px;
        font-size: 10.5px;
        font-weight: 700;
        letter-spacing: .4px;
        text-transform: uppercase;
        animation: mdaPop .35s ease both;
    }

    .mda-status-open {
        color: #c9740a;
        background: rgba(255, 159, 28, .15);
        animation: mdaPop .35s ease both, mdaStatusPulse 1.8s ease-in-out infinite;
    }

    .mda-status-approved {
        color: #1c8f84;
        background: rgba(46, 196, 182, .15);
    }

    .mda-status-rejected {
        color: #c9262a;
        background: rgba(229, 56, 59, .15);
    }

    @@keyframes mdaStatusPulse {
        0%, 100% {
            box-shadow: 0 0 0 0 rgba(255, 159, 28, .45);
        }

        50% {
            box-shadow: 0 0 0 6px rgba(255, 159, 28, 0);
        }
    }

    /* ---------- Remarks input ---------- */
    .mda-remarks-input {
        border-radius: 8px;
        border: 1px solid #dfe2ee;
        font-size: 12.5px;
        padding: 6px 10px;
        min-width: 160px;
        transition: box-shadow .15s ease, border-color .15s ease, background-color .15s ease;
    }

        .mda-remarks-input:focus {
            border-color: var(--mda-primary);
            box-shadow: 0 0 0 3px rgba(67, 97, 238, .15);
            outline: none;
        }

        .mda-remarks-input[readonly] {
            background: #f4f6fb;
            color: #8a8fa3;
            cursor: not-allowed;
        }

    /* ---------- Approve / Reject action buttons ---------- */
    .mda-action-cell {
        display: flex;
        gap: 8px;
        flex-wrap: nowrap;
    }

    .mda-btn-approve {
        background: linear-gradient(135deg, #2ec4b6, #21a99d);
    }

    .mda-btn-reject {
        background: linear-gradient(135deg, #e5383b, #c9262a);
    }

    /* ---------- Image preview modal ---------- */
    .mda-image-modal .modal-content {
        border: none;
        border-radius: 16px;
        overflow: hidden;
        animation: mdaPop .35s ease both;
    }

    .mda-image-modal .modal-header {
        background: linear-gradient(135deg, var(--mda-primary), var(--mda-primary-dark));
        color: #fff;
        border: none;
    }

        .mda-image-modal .modal-header .btn-close {
            filter: invert(1);
        }
    .mda-image-modal .modal-header {
        position: relative;
        z-index: 1056;
    }
    .mda-image-modal .modal-body {
        text-align: center;
        padding: 22px;
        min-height: 160px;
    }

        .mda-image-modal .modal-body img {
            max-width: 100%;
            max-height: 65vh;
            border-radius: 10px;
            box-shadow: 0 6px 20px rgba(20, 25, 60, .18);
            animation: mdaPop .4s ease both;
        }

    .mda-image-status {
        padding: 34px 0;
        color: #8a8fa3;
        font-size: 13.5px;
    }

        .mda-image-status i {
            font-size: 28px;
            margin-bottom: 10px;
            display: block;
        }

            .mda-image-status i.fa-spinner {
                animation: mdaPulseIcon 1s ease infinite;
                color: var(--mda-primary);
            }

            .mda-image-status i.fa-exclamation-triangle {
                color: var(--mda-warning);
            }
    .toastBox {
        pointer-events: none;
    }
</style>
<style>
    /* ---------- Amazon-style hover zoom ---------- */
    .mda-zoom-container {
        display: flex;
        gap: 20px;
        align-items: flex-start;
        min-height: 480px;
    }

    .mda-zoom-thumb-wrap {
        position: relative;
        width: 420px;
        height: 480px;
        border: 1px solid #eef0f7;
        border-radius: 12px;
        overflow: hidden;
        cursor: crosshair;
        flex: 0 0 auto;
        background: #f4f6fb;
    }

        .mda-zoom-thumb-wrap img {
            width: 100%;
            height: 100%;
            object-fit: contain;
            display: block;
        }

    .mda-zoom-lens {
        position: absolute;
        border: 2px solid var(--mda-primary);
        background: rgba(67, 97, 238, 0.15);
        width: 150px;
        height: 150px;
        pointer-events: none;
        display: none;
    }

    .mda-zoom-result {
        flex: 1 1 auto;
        min-width: 0;
        height: 480px;
        border: 1px solid #eef0f7;
        border-radius: 12px;
        background-repeat: no-repeat;
        background-color: #f4f6fb;
        display: none;
        box-shadow: 0 6px 20px rgba(20, 25, 60, .12);
    }

    .mda-zoom-thumbs-row {
        display: flex;
        gap: 10px;
        margin-top: 14px;
        flex-wrap: wrap;
    }

    .mda-zoom-thumb-small {
        width: 60px;
        height: 60px;
        border-radius: 8px;
        overflow: hidden;
        border: 2px solid transparent;
        cursor: pointer;
        flex: 0 0 auto;
        transition: border-color .15s ease;
    }

        .mda-zoom-thumb-small img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            display: block;
        }

        .mda-zoom-thumb-small.active {
            border-color: var(--mda-primary);
        }

    .mda-zoom-meta {
        margin-top: 10px;
        font-size: 13px;
        color: #6c7086;
    }

    .mda-image-modal .modal-dialog {
        max-width: 95vw;
    }

    @@media (max-width: 768px) {
        .mda-zoom-container {
            flex-direction: column;
        }

        .mda-zoom-result {
            display: none !important; /* zoom panel doesn't make sense on touch/mobile */
        }
    }
</style>


<body>
    <div class="mda-top-row">
        <div class="mda-header">
            <span class="mda-header-icon"><i class="fa fa-map-marker-alt"></i></span>
            <h4 class="mb-0" id="MainHeading">Customer Location Verification</h4>
        </div>

        <div class="mda-stats-row">
            <div class="mda-stat-card mda-card-total">
                <div class="mda-stat-icon"><i class="fa fa-list-alt"></i></div>
                <div class="mda-stat-body">
                    <span class="mda-stat-label">Total</span>
                    <span class="mda-stat-value" id="statTotal">0</span>
                </div>
            </div>
            <div class="mda-stat-card mda-card-open">
                <div class="mda-stat-icon"><i class="fa fa-clock-o"></i></div>
                <div class="mda-stat-body">
                    <span class="mda-stat-label">Open</span>
                    <span class="mda-stat-value" id="statOpen">0</span>
                </div>
            </div>
            <div class="mda-stat-card mda-card-active">
                <div class="mda-stat-icon"><i class="fa fa-check-circle"></i></div>
                <div class="mda-stat-body">
                    <span class="mda-stat-label">Approved</span>
                    <span class="mda-stat-value" id="statApproved">0</span>
                </div>
            </div>
            <div class="mda-stat-card mda-card-deactive">
                <div class="mda-stat-icon"><i class="fa fa-times-circle"></i></div>
                <div class="mda-stat-body">
                    <span class="mda-stat-label">Rejected</span>
                    <span class="mda-stat-value" id="statRejected">0</span>
                </div>
            </div>
        </div>
    </div>
    <hr />
    <div class="toastBox"></div>

    <div id="dvMainTable">

        <div class="row">
            <div class="table-responsive">
                <div class="d-flex align-items-center justify-content-start mb-2">
                    <div class="form-check form-switch m-0 ps-0 d-flex align-items-center">
                        <input class="form-check-input ms-0 me-2" type="checkbox" id="chkShowAll" style="cursor: pointer; transform: scale(1.4); margin-top: 0;">
                        <label class="form-check-label" for="chkShowAll" style="cursor: pointer; font-weight: 600; font-size: 16px;">
                            Show All
                        </label>
                    </div>
                </div>
                <table class="table table-bordered" id="tblCustomerLocation" width="100%">
                    <thead>
                        <tr>
                            <th style="text-align:center;width:0%;">ID</th>
                            <th style="text-align:center;width:0%;">StatusCode</th>
                            <th style="text-align:center;width:0%;">Latitude</th>
                            <th style="text-align:center;width:0%;">Longtitude</th>
                            <th style="text-align:left;width:0%;">Approved Rejected By</th>
                            <th style="text-align:center;width:0%;">Action Date Time</th>
                            <th style="text-align:left;width:22%;">Customer</th>
                            <th style="text-align:left;width:10%;">User</th>
                            <th style="text-align:center;width:10%;">Date Time</th>
                            <th style="text-align:center;width:8%;">Location</th>
                            <th style="text-align:center;width:8%;">Images</th>
                            <th style="text-align:center;width:10%;">Status</th>
                            <th style="text-align:left;width:10%;">Remarks</th>
                            <th style="text-align:left;width:12%;">Action</th>

                        </tr>
                    </thead>
                </table>
            </div>
        </div>
    </div>

    <!-- Image Preview Modal -->
    <div class="modal fade mda-image-modal" id="mdaImageModal" tabindex="-1" aria-labelledby="mdaImageModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered modal-xl">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="mdaImageModalLabel"><i class="fa fa-image"></i>&nbsp; Location Image</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body" id="mdaImageModalBody">
                    <div class="mda-image-status"><i class="fa fa-spinner fa-spin"></i>Loading image...</div>
                </div>
            </div>
        </div>
    </div>
</body>

<script>

var Table = $('#tblCustomerLocation').DataTable({
    lengthMenu: [[10, 20, 50, 100, -1], [10, 20, 50, 100, "All"]],
    "bFilter": true,
    "bSearchable": true,
    "bInfo": true,
    "bPaginate": false,
    "ordering": false,
    scrollY: "400px",
    scrollX: true,
    scrollCollapse: true,
    "columnDefs": [
        { "targets": [0,1, 2, 3, 4, 5], "visible": false },
    ],
});
var Aurl = '@Session["APIurl"]';
var UID = '@Session["LoginUserID"]';
var RID = '@Session["RoleID"]';
   
    function InitiateData() {
        var showAll = $('#chkShowAll').is(':checked') ? 1 : 0;
        var tblheight = "350px";
        var element = document.getElementById("chkShowAll");
        if (element) {
            var top = element.getBoundingClientRect().top + 120;
            tblheight = (window.innerHeight - top) + "px";
        }
        $.ajax({
            url: Aurl + "customermaster/CustomerLocation",
            type: 'GET',
            dataType: 'json',
            data: { ShowAll: showAll },
            xhrFields: {
                withCredentials: true
            },
            success: function (data) {
                var CustomerLocData = (data);
                UpdateStatCards(CustomerLocData);
                $('#tblCustomerLocation').DataTable({
                    lengthMenu: [[-1, 10, 20, 50, 100], ["All", 10, 20, 50, 100]],
                    scrollY: tblheight,
                    scrollX: true,
                    scrollCollapse: true,
                    paging: true,
                    destroy: true,
                    data: CustomerLocData,
                    fixedColumns: false,
                    searching: true,
                    ordering: true,
                    sorting: false,
                    columns: [
                        { "data": "ID", "autoWidth": true, "visible": false, className: "dt-body-left" },//0
                        { "data": "StatusID", "autoWidth": true, "visible": false, className: "dt-body-left" },//0
                        { "data": "Latitude", "autoWidth": true, "visible": false, className: "dt-body-left" },//0
                        { "data": "Longtitude", "autoWidth": true, "visible": false, className: "dt-body-left" },//0
                        { "data": "ApprovedOrRejectedBy", "autoWidth": true, "visible": false, className: "dt-body-left" },//0
                        { "data": "ApproveorRejectTime", "autoWidth": true, "visible": false, className: "dt-body-left" },//0
                        { "data": "Customer", width: "22%", "autoWidth": true, className: "dt-body-left" },//1
                        { "data": "User", width: "10%", "autoWidth": true, className: "dt-body-left" },//1
                        { "data": "DateTime", width: "12%", "autoWidth": true, className: "dt-body-left" },//1

                        {
                            //Location - 0 or 1
                            className: "dt-body-center", width: "8%",
                            "mRender": function (data, type, value, meta) {
                                var hasLocation = parseInt(value.Location, 10) === 1;
                                if (hasLocation) {
                                    var customerlocation = (value.Latitude || '').toString().trim() + "," + (value.Longtitude || '').toString().trim();
                                    return '<a href="javascript:void(0);" class="mda-link-btn mda-link-location gotolocation" data-location="' + customerlocation + '">'
                                        + '<i class="fa fa-map-marker-alt"></i> View</a>';
                                }
                                return '<span class="mda-empty-cell"><i class="fa fa-ban"></i> No Location</span>';
                            },
                        },
                        {
                            //Images - 0 or 1
                            className: "dt-body-center", width: "7%",
                            "mRender": function (data, type, value, meta) {
                                var hasImage = parseInt(value.Images, 10) === 1;
                                if (hasImage) {
                                    return '<button type="button" class="mda-link-btn mda-link-image btnViewImage" data-id="' + value.CustomerID + '" data-customername="' + value.Customer + '">'
                                        + '<i class="fa fa-image"></i> View</button>';
                                }
                                return '<span class="mda-empty-cell"><i class="fa fa-ban"></i> No Image</span>';
                            },
                        },
                        {
                            //Status - Open or Approved or Rejected
                            className: "dt-body-center", width: "10%",
                            "mRender": function (data, type, value, meta) {
                                var statusConfig = {
                                    1: { cls: 'mda-status-open', icon: 'fa-clock-o', label: 'Open' },
                                    2: { cls: 'mda-status-approved', icon: 'fa-check-circle', label: 'Approved' },
                                    3: { cls: 'mda-status-rejected', icon: 'fa-times-circle', label: 'Rejected' }
                                };
                                var statusId = parseInt(value.StatusID, 10);
                                var cfg = statusConfig[statusId] || { cls: '', icon: 'fa-question-circle', label: value.Status };

                                var infoMarkup = '';
                                // Info icon + custom animated tooltip, only for Approved (2) / Rejected (3).
                                // Label wording changes dynamically based on StatusID as required.
                                if (statusId === 2 || statusId === 3) {
                                    var byLabel = (statusId === 2) ? 'Approved By' : 'Rejected By';
                                    var timeLabel = (statusId === 2) ? 'Approved Time' : 'Reject Time';
                                    var byValue = (value.ApprovedOrRejectedBy && value.ApprovedOrRejectedBy !== 'NULL') ? value.ApprovedOrRejectedBy : '-';
                                    var timeValue = (value.ApproveorRejectTime && value.ApproveorRejectTime !== 'NULL') ? value.ApproveorRejectTime : '-';

                                    infoMarkup = '<span class="mda-status-tooltip-wrap">'
                                        + '<i class="fa fa-info-circle mda-status-info"></i>'
                                        + '<span class="mda-tooltip">'
                                        + '<span class="mda-tooltip-row"><i class="fa fa-user"></i><span>' + byLabel + ': ' + byValue + '</span></span>'
                                        + '<span class="mda-tooltip-row"><i class="fa fa-clock-o"></i><span>' + timeLabel + ': ' + timeValue + '</span></span>'
                                        + '</span>'
                                        + '</span>';
                                }

                                // Info icon is a flex-item INSIDE the badge itself, so it sits
                                // aligned with the badge text/icon rather than floating separately.
                                return '<span class="mda-status-badge ' + cfg.cls + '"><i class="fa ' + cfg.icon + '"></i> ' + cfg.label + infoMarkup + '</span>';
                            },
                        },
                        {
                            //Remarks
                            className: "dt-body-left", width: "10%",
                            "mRender": function (data, type, value, meta) {
                                var isReadonly = parseInt(value.StatusID, 10) === 3;
                                var remarkText = (value.Remarks && value.Remarks !== 'NULL') ? value.Remarks : '';
                                return '<input type="text" class="form-control mda-remarks-input remarkInput" data-id="' + value.ID + '" title="' + remarkText +'" value="' + remarkText + '" placeholder="Enter remarks..." ' + (isReadonly ? 'readonly' : '') + ' maxlength="50"/>';
                            },
                        },
                        {
                            //Action Button - Approve or Reject
                            className: "dt-body-left", width: "12%",
                            "mRender": function (data, type, value, meta) {
                                var statusId = parseInt(value.StatusID, 10);
                                var approveDisabled = (statusId === 2 || statusId === 3) ? 'disabled' : '';
                                var rejectDisabled = (statusId === 3) ? 'disabled' : '';
                                var approveLabel = (statusId === 2)
                                    ? '<i class="fa fa-check-circle"></i> Approved'
                                    : '<i class="fa fa-check"></i> Approve';
                                var rejectLabel = (statusId === 3)
                                    ? '<i class="fa fa-times-circle"></i> Rejected'
                                    : '<i class="fa fa-times"></i> Reject';

                                return '<div class="mda-action-cell">'
                                    + '<button type="button" class="mda-btn-icon mda-btn-approve btnApprove" data-id="' + value.ID + '" ' + approveDisabled + '>' + approveLabel + '</button>'
                                    + '<button type="button" class="mda-btn-icon mda-btn-reject btnReject" data-id="' + value.ID + '" ' + rejectDisabled + '>' + rejectLabel + '</button>'
                                    + '</div>';
                            },
                        },
                    ],

                });
                Table = $('#tblCustomerLocation').DataTable();
            }
        });
    }
    InitiateData();
    $('#chkShowAll').on('change', function () {
        InitiateData();
    });

    $(document).on("click", ".gotolocation", function (e) {
        e.preventDefault();
        var location = $(this).attr("data-location");

        if (!location) {
            if (typeof showErrorSnackbar === "function") {
                showErrorSnackbar("Location not available.");
            } else {
                alert("Location not available.");
            }
            return;
        }
        var url = "https://www.google.com/maps?q=" + encodeURIComponent(location);
        window.open(url, "_blank");
    });

    // Click handler for Approve button
    $('#tblCustomerLocation tbody').on('click', 'button.btnApprove:not(:disabled)', function () {
        var locationId = $(this).data('id');
        var remarks = $(this).closest('tr').find('.remarkInput').val();
        SaveCustomerLocationAction(2, locationId, remarks);
    });

    // Click handler for Reject button
    $('#tblCustomerLocation tbody').on('click', 'button.btnReject:not(:disabled)', function () {
        var locationId = $(this).data('id');
        var remarks = $(this).closest('tr').find('.remarkInput').val();
        SaveCustomerLocationAction(3, locationId, remarks);
    });

    // ---------- Stat cards ----------
    function UpdateStatCards(rows) {
        rows = rows || [];
        var openCount = 0, approvedCount = 0, rejectedCount = 0;
        $.each(rows, function (i, item) {
            var sid = parseInt(item.StatusID, 10);
            if (sid === 1) openCount++;
            else if (sid === 2) approvedCount++;
            else if (sid === 3) rejectedCount++;
        });
        $('#statTotal').text(rows.length);
        $('#statOpen').text(openCount);
        $('#statApproved').text(approvedCount);
        $('#statRejected').text(rejectedCount);
    }

    // ---------- Image preview modal ----------
    // Cache of already-fetched images, keyed by CustomerID, so re-opening the
    // same row's image doesn't call the server again.
    var customerimage = [];

    $(document).on('click', '.btnViewImage', function () {
        var customerId = $(this).data('id');
        var customername = $(this).data('customername');
        $('#mdaImageModalBody').html('<div class="mda-image-status"><i class="fa fa-spinner fa-spin"></i>Loading image...</div>');
        var mdaImageModalEl = document.getElementById('mdaImageModal');
        var mdaImageModal = bootstrap.Modal.getOrCreateInstance(mdaImageModalEl);
        mdaImageModal.show();

        $.ajax({
            url: Aurl + "customermaster/CustomerLocationImage",
            type: 'GET',
            dataType: 'json',
            data: { CustomerID: customerId },
            xhrFields: {
                withCredentials: true
            },
            success: function (data) {
                console.log("Img Data", data);
                RenderCustomerImage(data, customername);
            },
            error: function (xhr, status, error) {
                console.error("Error loading customer image:", error);
                $('#mdaImageModalBody').html('<div class="mda-image-status"><i class="fa fa-exclamation-triangle"></i>Failed to load image.</div>');
            }
        });
    });

    // Renders the CommonImagedata array (FileName, FileSize, CompressImageData)
    // as a gallery of thumbnails inside the popup. Hovering an image zooms it in.
    function RenderCustomerImage(images,customername) {
        if (!images || !images.length) {
            $('#mdaImageModalBody').html('<div class="mda-image-status"><i class="fa fa-exclamation-triangle"></i>No image found for this customer.</div>');
            return;
        }

        var html = '<div class="mda-zoom-container">'
            + '<div class="mda-zoom-thumb-wrap" id="mdaZoomThumbWrap">'
            + '<img id="mdaZoomImg" src="" alt="" />'
            + '<div class="mda-zoom-lens" id="mdaZoomLens"></div>'
            + '</div>'
            + '<div class="mda-zoom-result" id="mdaZoomResult"></div>'
            + '</div>'
            + '<div class="mda-zoom-meta" id="mdaZoomMeta"></div>';

        if (images.length > 1) {
            html += '<div class="mda-zoom-thumbs-row" id="mdaZoomThumbsRow"></div>';
        }

        $('#mdaImageModalBody').html(html);

        // Normalize each image's src/fileName/fileSize once
        var normalized = $.map(images, function (img, i) {
            var raw = img.CompressImageData || img.ImgData || '';
            var src = (raw.indexOf('data:image') === 0) ? raw : ('data:image/jpeg;base64,' + raw);
            return {
                src: src,
                fileName: img.FileName || ('Image ' + (i + 1)),
                fileSize: img.FileSize || ''
            };
        });

        SetActiveZoomImage(normalized[0],customername);
        InitZoom();

        if (normalized.length > 1) {
            var rowHtml = '';
            $.each(normalized, function (i, item) {
                rowHtml += '<div class="mda-zoom-thumb-small' + (i === 0 ? ' active' : '') + '" data-index="' + i + '">'
                    + '<img src="' + item.src + '" alt="' + item.fileName + '" />'
                    + '</div>';
            });
            $('#mdaZoomThumbsRow').html(rowHtml);

            $('#mdaZoomThumbsRow').on('click', '.mda-zoom-thumb-small', function () {
                var idx = $(this).data('index');
                $('#mdaZoomThumbsRow .mda-zoom-thumb-small').removeClass('active');
                $(this).addClass('active');
                SetActiveZoomImage(normalized[idx], customername);
            });
        }
    }

    function SetActiveZoomImage(item, customername) {
        $('#mdaZoomImg').attr('src', item.src).attr('alt', item.fileName);
        $('#mdaZoomResult').css('background-image', 'url(' + item.src + ')');
        $('#mdaZoomMeta').html('<strong><i class="fa fa-user"></i> ' + customername + '</strong> &middot; <strong><i class="fa fa-file-image-o"></i> ' + item.fileName + '</strong>' + (item.fileSize ? ' &middot; ' + item.fileSize : ''));
    }

    // Sets up the lens-follow-cursor zoom interaction. Bound once per modal open
    // since the elements are freshly injected each time.
    function InitZoom() {
        var $wrap = $('#mdaZoomThumbWrap');
        var $img = $('#mdaZoomImg');
        var $lens = $('#mdaZoomLens');
        var $result = $('#mdaZoomResult');

        $wrap.off('mouseenter mousemove mouseleave');

        $wrap.on('mouseenter', function () {
            if (window.innerWidth <= 768) return; // skip on small screens
            $lens.show();
            $result.show();
        });

        $wrap.on('mouseleave', function () {
            $lens.hide();
            $result.hide();
        });

        $wrap.on('mousemove', function (e) {
            if (window.innerWidth <= 768) return;

            var wrapOffset = $wrap.offset();
            var wrapW = $wrap.width();
            var wrapH = $wrap.height();
            var lensW = $lens.width();
            var lensH = $lens.height();

            var x = e.pageX - wrapOffset.left - lensW / 2;
            var y = e.pageY - wrapOffset.top - lensH / 2;

            // Clamp lens inside the thumbnail bounds
            x = Math.max(0, Math.min(x, wrapW - lensW));
            y = Math.max(0, Math.min(y, wrapH - lensH));

            $lens.css({ left: x + 'px', top: y + 'px' });

            // Map lens position to the zoomed background position
            var resultW = $result.width();
            var resultH = $result.height();
            var ratioX = resultW / lensW;
            var ratioY = resultH / lensH;

            $result.css({
                backgroundSize: (wrapW * ratioX) + 'px ' + (wrapH * ratioY) + 'px',
                backgroundPosition: '-' + (x * ratioX) + 'px -' + (y * ratioY) + 'px'
            });
        });
    }

    //function SaveCustomerLocationAction(Mode, LocationID, Remarks) {
    //    $.ajax({
    //        url: Aurl + "customermaster/CustomerLocationVerificationSave",
    //        type: 'POST',
    //        dataType: 'json',
    //        data: {
    //            mode: Mode,
    //            id: LocationID,
    //            userId: UID,
    //            remarks: Remarks || ''
    //        },
    //        xhrFields: {
    //            withCredentials: true
    //        },
    //        success: function (data) {
    //            $.each(data, function (i, items) {
    //                if (items.MsgID == "0" || items.Status == "Success") {
    //                    if (Mode == 2) {
    //                        showSuccessSnackbar("Approved Successfully.!!!");
    //                    } else {
    //                        showSuccessSnackbar("Rejected Successfully.!!!");
    //                    }
    //                    setTimeout(() => {
    //                        InitiateData();
    //                    }, 300);
    //                } else {
    //                    showErrorPopup(items.Message || "Operation failed.");
    //                }
    //            });
    //        },
    //        error: function (xhr, status, error) {
    //            console.error("Error saving status:", error);
    //            showErrorSnackbar("An error occurred while saving.");
    //        }
    //    });
    //}

    function SaveCustomerLocationAction(Mode, LocationID, Remarks) {
        $.ajax({
            url: Aurl + "customermaster/CustomerLocationVerificationSave?mode=" + Mode + "&id=" + LocationID + "&userId=" + UID + "&remarks=" + encodeURIComponent(Remarks || ''),
            type: 'POST',
            dataType: 'json',
            xhrFields: {
                withCredentials: true
            },
            success: function (data) {
                $.each(data, function (i, items) {
                    if (items.MsgID == "0" || items.Status == "Success") {
                        if (Mode == 2) {
                            showSuccessSnackbar("Approved Successfully.!!!");
                        } else {
                            showSuccessSnackbar("Rejected Successfully.!!!");
                        }
                        setTimeout(() => {
                            InitiateData();
                        }, 300);
                    } else {
                        showErrorPopup(items.Message || "Operation failed.");
                    }
                });
            },
            error: function (xhr, status, error) {
                console.error("Error saving status:", error);
                showErrorSnackbar("An error occurred while saving.");
            }
        });
    }
</script>