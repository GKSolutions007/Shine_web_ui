@model ShineWeb.Models.SingleMasterModel
@{
    ViewBag.Title = ViewData["FormName"];
    Layout = "~/Views/Shared/_LayoutBT5.cshtml";
}
<link href="~/Content/5.3.8/bootstrap.min.css" rel="stylesheet" />
<link href="~/Content/navbar.css" rel="stylesheet" />
<link href="~/Content/buttonTop.css" rel="stylesheet" />
<link href="~/Content/dataTables.dataTables.2.2.2.css" rel="stylesheet" />
<link href="~/GKBS_Grid/css/gridui.css" rel="stylesheet" />

<script src="~/Scripts/5.3.8/bootstrap.min.js"></script>
<script src="~/Scripts/jquery-1.10.2.min.js"></script>
<script src="~/Scripts/jquery-3.7.1.js"></script>
<script src="~/Scripts/jquery-ui.js"></script>
<script src="~/Scripts/dataTables.2.2.2.js"></script>
<script src="~/Scripts/js/refreshtoken.js"></script>
<script src="~/GKBS_Grid/js/gridui.js"></script>
<style>  

    td {
        font-size: 11px;
    }

    input[type=number] {
        text-align: right;
    }

        input[type=number]::placeholder {
            text-align: left;
        }
    /* Chrome, Safari, Edge, Opera */
    input::-webkit-outer-spin-button,
    input::-webkit-inner-spin-button {
        -webkit-appearance: none;
        margin: 0;
    }

    /* Firefox */
    input[type=number] {
        -moz-appearance: textfield;
    }
    /* The Modal (background) */
    .modalBatch {
        display: none; /* Hidden by default */
        position: fixed; /* Stay in place */
        z-index: 200; /* Sit on top */
        left: 0;
        top: 0;
        width: 100%; /* Full width */
        height: 100%; /* Full height */
        overflow: auto; /* Enable scroll if needed */
        background-color: rgb(0,0,0); /* Fallback color */
        background-color: rgba(0,0,0,0.4); /* Black w/ opacity */
    }

    /* Modal Content/Box */
    @@media only screen and (min-width: 600px) {
        .modalBatch-content {
            background-color: #ffffff;
            margin: 3% auto; /* 15% from the top and centered */
            padding: 10px;
            border: 1px solid #888;
            width: 35%; /* Could be more or less, depending on screen size */
            height: 70%;
            top: 5%;
            overflow: auto;
        }
    }

    @@media only screen and (max-width: 600px) {
        .modalBatch-content {
            background-color: #ffffff;
            margin: 10% auto; /* 15% from the top and centered */
            padding: 25px;
            border: 1px solid #888;
            width: 95%; /* Could be more or less, depending on screen size */
            height: 80%;
            top: 20%;
            overflow: auto;
        }
    }
    /* The Close Button */
    .closefg {
        color: #aaa;
        float: right;
        font-size: 10px;
        font-weight: bold;
    }

        .closefg:hover,
        .closefg:focus {
            color: black;
            text-decoration: none;
            cursor: pointer;
        }

    .form-label {
        position: relative;
        display: block;
        clear: both;
        max-width: 500px;
        margin: 0 auto;
    }

    .labelup {
        -webkit-transform: translateY(50%);
        -moz-transform: translateY(-50%);
        -ms-transform: translateY(-50%);
        -o-transform: translateY(-50%);
        transform: translateY(50%);
        -webkit-transition: all 0.2s ease-in-out;
        -moz-transition: all 0.2s ease-in-out;
        transition: all 0.2s ease-in-out;
        position: absolute;
        top: -50%;
        left: 1em;
        background: #fffefe;
        color: #2b2828;
        font-size: 0.90em;
        cursor: text;
        pointer-events: none;
        font-family: sans-serif;
    }
</style>
<body>

    @Html.HiddenFor(model => model.Add, new { @autocomplete = "off", @class = "form-control", @id = "txtAddID" })
    @Html.HiddenFor(model => model.Modify, new { @autocomplete = "off", @class = "form-control", @id = "txtModifyID" })
    @Html.HiddenFor(model => model.View, new { @autocomplete = "off", @class = "form-control", @id = "txtViewID" })
    @Html.HiddenFor(model => model.FormName, new { @autocomplete = "off", @class = "form-control", @id = "txtFormName" })
    @Html.HiddenFor(model => model.ID, new { @autocomplete = "off", @class = "form-control FormclrField", @id = "txtID" })

    <div class="d-flex align-items-center gap-2">
        <!-- Larger heading -->
        <h4 class="mb-0" style="font-size:14px; padding-top:10px;" id="MainHeading">@ViewData["FormName"]</h4>
    </div>
    <hr />
    <div class="toastBox"></div>
    <div id="dvMainTable">
        <div id="my-grid" style="height: 600px; width: 100%;"></div>
    </div>

    <div class="modal fade" id="myModalfg" tabindex="-1" data-bs-backdrop="static" data-bs-keyboard="false">
        <div class="modal-dialog modal-sm">
            <div class="modal-content">

                <!-- Modal Header -->
                <div class="modal-header">
                    <h5 class="modal-title fw-bold" id="lblPopupheading">
                        Add @ViewData["FormName"]
                    </h5>

                    <button type="button" class="btn-close" id="modelcloseX"></button>
                </div>

                <!-- Modal body -->
                <div class="modal-body">
                    <div class="text-center pt-1">                      
                            <table class="table w-100">

                                <tr>
                                    <td class="border-0 px-2" colspan="2">

                                        <span class="form-label">
                                            <input tabindex="1" type="text" id="txtName"
                                                   class="form-control FormclrField"
                                                   onbeforeinput="return AlphaNumericBeforeInput(event, this,'#dvErrorName')"
                                                   oninput="sanitizeAlphaNumeric(this, '#dvErrorName')" />

                                            <label class="labelup">Service Name *</label>
                                        </span>

                                        <div class="clrdiv text-danger" id="dvErrorName"></div>
                                    </td>
                                </tr>

                                <tr>
                                    <td class="border-0 px-2" colspan="2">

                                        <span class="form-label">
                                            <select tabindex="2" id="cmbTax" class="form-control"></select>
                                            <label class="labelup">Tax *</label>
                                        </span>

                                        <div class="clrdiv text-danger" id="dvErrorTax"></div>
                                    </td>
                                </tr>

                                <tr>
                                    <td class="border-0 px-2" colspan="2">

                                        <span class="form-label">
                                            <input tabindex="3" type="number"
                                                   id="txtSAC"
                                                   class="form-control FormclrField"
                                                   maxlength="6"
                                                   oninput="if (this.value.length > this.maxLength) { this.value = this.value.slice(0, this.maxLength); }" />

                                            <label class="labelup">SAC Code</label>
                                        </span>

                                        <div class="clrdiv text-danger" id="dvErrorSAC"></div>
                                    </td>
                                </tr>

                                <tr>
                                    <td class="border-0 px-2" colspan="2">

                                        <span class="form-label">
                                            <input tabindex="4" type="number"
                                                   txttag="0"
                                                   id="txtInwardPrice"
                                                   class="form-control FormclrField txtsettag"
                                                   onbeforeinput="return NumericOnlyBeforeInput(event, this,'#dvErrorInwardPrice')"
                                                   oninput="sanitizeNumericOnly(this, '#dvErrorInwardPrice')" />

                                            <label class="labelup">Inward Price</label>
                                        </span>

                                        <div class="clrdiv text-danger" id="dvErrorInwardPrice"></div>
                                    </td>
                                </tr>

                                <tr>
                                    <td class="border-0 px-2" colspan="2">

                                        <span class="form-label">
                                            <input tabindex="5" type="number"
                                                   txttag="0"
                                                   id="txtOutwardPrice"
                                                   class="form-control FormclrField txtsettag"
                                                   onbeforeinput="return NumericOnlyBeforeInput(event, this,'#dvErrorOutwardPrice')"
                                                   oninput="sanitizeNumericOnly(this, '#dvErrorOutwardPrice')" />

                                            <label class="labelup">Outward Price</label>
                                        </span>

                                        <div class="clrdiv text-danger" id="dvErrorOutwardPrice"></div>
                                    </td>
                                </tr>

                                <tr>
                                    <td class="border-0 text-start ps-2" colspan="2">
                                        <label for="chkActive">Active :</label>
                                        <input tabindex="6" type="checkbox" id="chkActive" checked disabled />
                                        <div class="clrdiv text-danger" id="dvErrorActive"></div>
                                    </td>
                                </tr>

                            </table>

                       
                    </div>

                </div>

                <!-- Modal footer -->
                <div class="modal-footer">

                    <div class="w-100 text-end">

                        <button tabindex="8" type="button" class="btn btn-danger" id="btnClear" style="padding:5px 10px;font-size:13px;font-weight:600;color:white">
                           X&ensp;Clear
                        </button>

                        <button tabindex="7" type="button" class="btn btn-success" id="btnSave" style="padding:5px 10px;font-size:13px;font-weight:600;color:white">
                            <span class="glyphicon glyphicon-saved"> Save</span>
                        </button>

                        <button tabindex="9" type="button" class="btn btn-warning" id="btnClose" style="padding:7px 10px;font-size:13px;font-weight:600;color:white">
                            <i class="bi bi-x-circle"></i> Close
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>


<script>
    //~Filter Action Buttons
    const allGridActions = [
        {
            icon: '👁️',
            title: 'View Data',
            className: 'dg-action-view',
            // 💡 NEW TAG: This action will be rendered as a single icon
            displayType: 'single_icon',
            visible: $("#txtViewID").val() == 1,
            handler: (rowData) => {
                var ID = rowData.ID;//[0];
                GetData(4, ID);
                $(".FormclrField").prop("disabled", true);
                $(".modal-title#lblPopupheading").text("View " + $("#MainHeading").text());
                $("#chkActive,#btnSave,#btnClear,#cmbTax").prop("disabled", true);
                modal.show();
            }
        },
        {
            icon: '✏️',
            title: 'Edit Data',
            className: 'dg-action-edit',
            visible: $("#txtModifyID").val() == 1,
            displayType: 'single_icon',
            handler: (rowData, gridInstance) => {
                $(".clrdiv").html("");
                nMode = 2;
                var ID = rowData.ID;//[0];
                GetData(4, ID);
                $(".FormclrField").prop("disabled", false);
                $("#chkActive,#btnSave,#cmbTax").prop("disabled", false);
                $("#btnClear").prop("disabled", true);
                $(".modal-title#lblPopupheading").text("Edit " + $("#MainHeading").text());
                modal.show();
            }
        }]
    var columns = [

    ];
    const singleIconAction = allGridActions.filter(a => a.displayType === 'single_icon');
    const menuActions = [];// allGrid3dotActions.filter(a => a.displayType === 'menu_item');
    var actions = {
        "field": "Action",
        "header": "Action",
        "type": "action",
        "width": 80,
        "align": "center",
        "visible": true,
        "specialColumn": true,
        "ShowinColumnOption": false,
        "actions": allGridActions
    };
</script>

<script type="text/javascript">

    var Aurl = '@Session["APIurl"]';
    var grid, Services;
    GetColumnSettingsData(2, 3, 1);
    function displayResult(item) {
        $('.alert').show().html('You selected <strong>' + item.value + '</strong>: <strong>' + item.text + '</strong>');
    }
    var arrData = [];
    var UID = '@Session["LoginUserID"]';
    var nMode = 1;
    var FID = $("#txtFormID").val();
        var DecVal = '@Session["DecimalValues"]';

    GetData(3);
    GetData(5);
    //~Grid Data
    function GetData(nMode,nID = 0) {
        $.ajax({
            url: Aurl + "service/get",
            type: 'get',
            contentType: 'json',
            data: { FormID: FID, Mode: nMode, Name: nID },
            xhrFields: {
                withCredentials: true
            },
            success: function (data) {
                if (nMode == 3) {
                    if (data.length == 0) {
                        showErrorSnackbar("No Records Found");
                    }
                    Services = data
                        arrData = [];
                        $.each(data, function (i, items) {
                            //arrData[i] = items.Name
                            var al = {
                                "label": items.Name,
                                "active": items.Active,
                                "id": items.ID,
                            }
                            arrData.push(al);
                        });

                        var currentSearchValue = ''; if (grid) { currentSearchValue = grid ? grid.state.searchTerm : ''; } grid = new GKBSDynamicGrid('#my-grid', columns, Services, {
                            enablePagination: true,
                            pageSize: ItemsperPage,
                            enableSearch: true,
                            stickyToWindow: true,
                            enableExport: false,
                            enablePrint: false,
                            enableColumnsBtn: false,
                            enableAddRow: false,
                            enableRemoveRow: false,
                            enableSorting: true,
                            searchTerm: currentSearchValue, singleIconAction: singleIconAction, // Pass the single View action
                            rowActionsMenu: menuActions,       // Pass the Delete/Print actions
                            //onCellChange: handleCellUpdate,
                            customCellRenderer: (el, col, rowData) => {
                                if (col.field === 'Active') {
                                    const val = rowData[col.field];
                                    console.log("Active : ", val);
                                    if (val === 'True') {
                                        el.innerHTML = `<div class="dg-status-badge dg-status-active">
                      <span class="dg-status-circle"></span> Active
                  </div>`;
                                    } else if (val === 'False') {
                                        el.innerHTML = `<div class="dg-status-badge dg-status-inactive">
                      <span class="dg-status-circle"></span> In-Active
                  </div>`;
                                    }
                                }
                            },
                            customButtons: [
                                {
                                    icon: '⚙️',
                                    text: 'Settings',
                                    title: 'Settings',
                                    visible: true,
                                    onClick: (gridInstance, event) => {
                                        modalGenColumnSettings.show();
                                        GendralColumnSettings(1, 3, 1, 0);
                                    }
                                },
                                {
                                    icon: '➕',
                                    text: 'Add New',
                                    title: 'Add New',
                                    visible: $("#txtAddID").val() == 1,
                                    onClick: (gridInstance, event) => {
                                        nMode = 1;
                                        $("#chkActive,#btnSave,#cmbTax,#btnClear,.FormclrField").prop("disabled", false);
                                        $(".modal-title#lblPopupheading").text("Add " + $("#MainHeading").text());
                                        modal.show();
                                        ClearForm();
                                    }
                                },
                            ],
                            onCellClick: (rowData, fieldName, cellValue, column) => {

                            },
                            onCellDoubleClick: (rowData, fieldName, cellValue, column) => {
                            },
                            onColumnResize: (colIndex, field, header, newWidth) => {
                                UpdateColumnWidth(3, 1, 0, colIndex, field, newWidth)
                            },
                        });
                    //~AutoList
                    PaginatedAutocomplete({
                        inputId: "txtName",
                        data: arrData,
                        onSelect: function (item) {
                            $("#txtName").val(item.label);
                            return false;
                        }
                    });
                        //$("#txtName").autocomplete({
                        //    minLength: 0,
                        //    source: arrData,
                        //    select: function (event, ui) {
                        //        $("#txtName").val(ui.item.label);
                        //        return false;
                        //    },
                        //}).autocomplete("instance")._renderItem = function (ul, item) {
                        //    return $("<li class='each'>")
                        //        .append("<div class='acItem'><span class='name'>" +
                        //            item.label + "</span></div>")
                        //        .appendTo(ul);
                        //};
                }
                    //~Assign Data
                    else if (nMode == 4) {
                        $.each(data, function (i, items) {
                            $("#txtID").val(items.ID);
                            $("#txtName").val(items.Name);
                            $("#txtSAC").val(items.SACCode);
                            $("#cmbTax").val(items.TaxID);
                            updateInputColumn("txtInwardPrice", items.Inwardprice);
                            updateInputColumn("txtOutwardPrice", items.Outwardprice);
                            $("#chkActive").prop("checked", items.Active == "True");
                        });
                    }
                    else {
                        $('#cmbTax').find("option").remove();
                        $('#cmbTax').append($('<option/>', {
                            value: 0,
                            text: "-- Select Tax --",
                            class: ""
                        }));
                        $.each(data, function (i, items) {
                            $('#cmbTax').append($('<option/>', {
                                value: items.ID,
                                text: items.Name,
                            }));
                        });
                    }

            }, error: function (data) {
                if (data.status == "401") {
                    var msg = data.responseJSON.Message;
                    //showErrorSnackbar(msg + " Logout and Login again.");
                }
            }
        });
    }
    //~SETTag for Textbox
function updateInputColumn(inputID, netValue) {
    //const label = document.getElementById(`dgvProdGross${rowId}`);
    const input = document.getElementById(inputID);
    var value = netValue != "" ? netValue : 0;
    input.setAttribute('txttag', parseFloat(value).toFixed(4));
    input.value = parseFloat(value).toFixed(DecVal);
    input.title = netValue;
}
    $(".txtsettag").focusin(function (e) {
        var id = e.target.id;
        const val = document.getElementById(id).getAttribute("txttag");
        $("#" + id).val(val);
        $("#" + id).select();
    });
    $(".txtsettag").blur(function (e) {
        var val = e.target.value;
        var id = e.target.id;
        updateInputColumn(id, val);
    });
    function SetTagZero() {
        var inputs = $(".txtsettag");
        for (var i = 0; i < inputs.length; i++) {
            var field = $(inputs[i]);
            var FID = field[0].id;
            updateInputColumn(FID, "0");
        }
    }
    var modal = new bootstrap.Modal(document.getElementById('myModalfg')); //document.getElementById("myModalfg");

    //~Close
    $('#btnClose,#modelcloseX').click(function (e) {
        if (ClosecfFocus != 3) { CloseConfirmDialogMsg("Do you want to close?"); } else { CloseForm(); }
    });
    function CloseForm() {
        modal.hide();
    }
    //~Clear
    $('#btnClear').click(function (e) {
        if (cfFocus != 3) { ClearConfirmDialogMsg("Do you want to clear?"); } else { ClearForm(); }
    });
    function ClearForm() {
        $(".FormclrField").val("");
        $("#cmbTax").val(0)
        $("#chkActive").prop("checked", true);
        $("#chkActive").prop("disabled", nMode != 2);
        SetTagZero();
        $(".clrdiv").html("");
        $("#txtName").focus();

        if (nMode != 1)
            modal.hide();
    }
    //~Save
    $("#btnSave").click(function () {
        $(".clrdiv").html("");
        //~Validation
        var IsValid = true, ChkFormation = true;
        if ($("#txtName").val() == "") {
            IsValid = false;
            $("#dvErrorName").html("Name should not be empty");
            showErrorSnackbar("Name should not be empty");
        }
        if ($("#cmbTax").val() == 0) {
            IsValid = false;
            $("#dvErrorTax").html("Select Tax");
            showErrorSnackbar("Select Tax");
        }
        ChkFormation = SACValidation($("#txtSAC"), $("#dvErrorSAC"));
        if (!ChkFormation) {
            IsValid = false;
        }
        if (IsValid) {
            if (cfFocus != 3) { ConfirmDialogMsg("Do you want to save data?"); } else { SaveData(); }
        }
    });
    function SaveData() {
        $(".clrdiv").html("");
        $("#btnSave").prop("disabled", true);
        var lstMaster = {
            "FormID": FID,
            "Mode": nMode,
            "ID": $("#txtID").val(),
            "Name": $("#txtName").val(),
            "TaxID": $("#cmbTax").val(),
            "SACCode": $("#txtSAC").val(),
            "Inwardprice": document.getElementById("txtInwardPrice").getAttribute("txttag"),//$("#txtInwardPrice").val(),
            "Outwardprice": document.getElementById("txtOutwardPrice").getAttribute("txttag"),//$("#txtOutwardPrice").val(),
            "Active": $("#chkActive").is(":checked") ? 1 : 0,
            "UserID": UID
        };
        $.ajax({
            url: Aurl + "service/save",
            type: 'POST',
            contentType: 'application/json; charset=utf-8',
            data: JSON.stringify(lstMaster),
            cache: false,
            processData: false,
            xhrFields: {
                withCredentials: true
            },
            success: function (data) {
                $("#btnSave").prop("disabled", false);
                $.each(data, function (i, items) {
                    if (items.MsgID == 0) {
                        showSuccessSnackbar(items.Message);
                        ClearForm();
                        GetData(3);
                        modal.hide();
                    }
                    else {
                        $("#dvErrorName").html(items.Message);
                        showErrorSnackbar(items.Message);
                    }
                });
            }, error: function (data) {
                $("#btnSave").prop("disabled", false);
                if (data.status == "401") {
                    var msg = data.responseJSON.Message;
                    //showErrorSnackbar(msg + " Logout and Login again.");
                }
            }
        });
    }    
</script>