@model ShineWeb.Models.SingleMasterModel
@{
    ViewBag.Title = ViewData["FormName"];
    Layout = "~/Views/Shared/_LayoutBT5.cshtml";
}

<link href="~/Content/navbar.css" rel="stylesheet" />
<link href="~/Content/buttonTop.css" rel="stylesheet" />
@*<link href="~/Content/5.3.8/bootstrap.min.css" rel="stylesheet" />*@
<link href="~/Content/dataTables.bootstrap.min.css" rel="stylesheet" />
<link href="~/Content/dataTables.dataTables.2.2.2.css" rel="stylesheet" />
<link href="~/GKBS_Grid/css/gridui.css" rel="stylesheet" />

<script src="~/Scripts/jquery-1.11.1.min.js"></script>
<script src="~/Scripts/5.3.8/bootstrap.min.js"></script>
<script src="~/Scripts/jquery-3.7.1.js"></script>
<script src="~/Scripts/jquery-ui.js"></script>
<script src="~/Scripts/dataTables.2.2.2.js"></script>
<script src="~/Scripts/js/refreshtoken.js"></script>
<script src="~/GKBS_Grid/js/gridui.js"></script>


<header>
    <link rel="stylesheet" href="~/Content/index.css">
</header>

<style>
    th, td {
        white-space: nowrap;
    }
    hr {
        margin: 1rem 0;
        color: inherit;
        border: 0;
        border-top: 1px solid;
        opacity: .25;
    }
    .gdInput {
        width: 100%;
        height: 25px;
        border-width: 0 0 0.1px 0;
        border-color: #e0d6d6;
    }

    .trtext-line {
        border: none;
        border-bottom: 1px #000000;
    }

    div.dataTables_wrapper {
        width: 100%;
        margin: 0 auto;
    }

    td {
        font-size: 11px;
    }

    input[type=number] {
        text-align: right;
    }

        input[type=number]::placeholder {
            text-align: left;
        }
    /* Chrome, Safari, Edge, Opera */
    input::-webkit-outer-spin-button,
    input::-webkit-inner-spin-button {
        -webkit-appearance: none;
        margin: 0;
    }

    /* Firefox */
    input[type=number] {
        -moz-appearance: textfield;
    }
    /* The Modal (background) */
    .modalBatch {
        display: none; /* Hidden by default */
        position: fixed; /* Stay in place */
        z-index: 200; /* Sit on top */
        left: 0;
        top: 0;
        width: 100%; /* Full width */
        height: 100%; /* Full height */
        overflow: auto; /* Enable scroll if needed */
        background-color: rgb(0,0,0); /* Fallback color */
        background-color: rgba(0,0,0,0.4); /* Black w/ opacity */
    }

    /* Modal Content/Box */
    @@media only screen and (min-width: 600px) {
        .modalBatch-content {
            background-color: #ffffff;
            margin: 3% auto; /* 15% from the top and centered */
            padding: 10px;
            border: 1px solid #888;
            width: 75%; /* Could be more or less, depending on screen size */
            height: 70%;
            top: 5%;
            overflow: auto;
        }
    }

    @@media only screen and (max-width: 600px) {
        .modalBatch-content {
            background-color: #ffffff;
            margin: 10% auto; /* 15% from the top and centered */
            padding: 25px;
            border: 1px solid #888;
            width: 95%; /* Could be more or less, depending on screen size */
            height: 80%;
            top: 20%;
            overflow: auto;
        }
    }
    /* The Close Button */
    .closefg {
        color: #aaa;
        float: right;
        font-size: 10px;
        font-weight: bold;
    }

        .closefg:hover,
        .closefg:focus {
            color: black;
            text-decoration: none;
            cursor: pointer;
        }

    .modalProdInfo {
        display: none; /* Hidden by default */
        position: fixed; /* Stay in place */
        z-index: 10; /* Sit on top */
        left: 0;
        top: 0;
        width: 100%; /* Full width */
        height: 100%; /* Full height */
        overflow: auto; /* Enable scroll if needed */
        background-color: rgb(0,0,0); /* Fallback color */
        background-color: rgba(0,0,0,0.4); /* Black w/ opacity */
    }

    /* Modal Content/Box */
    @@media only screen and (min-width: 600px) {
        .modalProdInfo-content {
            background-color: #ffffff;
            margin: 3% auto; /* 15% from the top and centered */
            padding: 10px;
            border: 1px solid #888;
            width: 75%; /* Could be more or less, depending on screen size */
            height: 70%;
            top: 5%;
            overflow: auto;
        }
    }

    @@media only screen and (max-width: 600px) {
        .modalProdInfo-content {
            background-color: #ffffff;
            margin: 10% auto; /* 15% from the top and centered */
            padding: 25px;
            border: 1px solid #888;
            width: 95%; /* Could be more or less, depending on screen size */
            height: 80%;
            top: 20%;
            overflow: auto;
        }
    }
    /* The Close Button */
    .closePinfo {
        color: #aaa;
        float: right;
        font-size: 10px;
        font-weight: bold;
    }

        .closePinfo:hover,
        .closePinfo:focus {
            color: black;
            text-decoration: none;
            cursor: pointer;
        }

    /* Style the tab */
    .tab {
        overflow: hidden;
        border: 1px solid #ccc;
        background-color: #f1f1f1;
    }

        /* Style the buttons inside the tab */
        .tab button {
            background-color: inherit;
            float: left;
            border: none;
            outline: none;
            cursor: pointer;
            padding: 14px 16px;
            transition: 0.5s;
            font-size: 17px;
        }

            /* Change background color of buttons on hover */
            .tab button:hover {
                background-color: #ddd;
            }

            /* Create an active/current tablink class */
            .tab button.active {
                background-color: #ccc;
            }

    /* Style the tab content */
    .tabcontent {
        display: none;
        /*padding: 6px 12px;*/
        border: none;
        border-top: none;
    }

    .form-label {
        position: relative;
        display: block;
        clear: both;
        max-width: 500px;
        margin: 0 auto;
    }

    .labelup {
        -webkit-transform: translateY(50%);
        -moz-transform: translateY(-50%);
        -ms-transform: translateY(-50%);
        -o-transform: translateY(-50%);
        transform: translateY(50%);
        -webkit-transition: all 0.2s ease-in-out;
        -moz-transition: all 0.2s ease-in-out;
        transition: all 0.2s ease-in-out;
        position: absolute;
        top: -60%;
        left: 1em;
        background: #fffefe;
        color: #2b2828;
        font-size: 0.9em;
        font-weight: 600;
        letter-spacing: 1px;
        cursor: text;
        pointer-events: none;
        font-family: sans-serif;
    }

    .labelupfortextarea {
        -webkit-transform: translateY(50%);
        -moz-transform: translateY(-50%);
        -ms-transform: translateY(-50%);
        -o-transform: translateY(-50%);
        transform: translateY(50%);
        -webkit-transition: all 0.2s ease-in-out;
        -moz-transition: all 0.2s ease-in-out;
        transition: all 0.2s ease-in-out;
        position: absolute;
        top: -28%;
        left: 1em;
        background: #fffefe;
        color: #2b2828;
        font-size: 0.9em;
        font-weight: 600;
        letter-spacing: 1px;
        cursor: text;
        pointer-events: none;
        font-family: sans-serif;
    }

    .footerAction {
        position: fixed;
        right: 0;
        bottom: 0;
        width: 85%;
        color: white;
        background-color: azure;
    }
    /*input:focus + .labelup,
    input:not(:placeholder-shown) + .labelup, textarea:focus + .labelup, textarea:not(:placeholder-shown) + .labelup {
        top: -35%;
        font-size: .9em;
        padding: 0 .3em;
        background: #F9F9F9;
        color: #000000;
    }*/
    .clsProd {
        position: fixed;
        right: 1%;
        bottom: 8%;
        width: 30%;
        height: 45%;
        color: black;
        background-color: #c2f6b5;
        z-index: 1000;
    }

    /* ===== Compact Input Styling ===== */
    .form-control,
    select.form-control,
    input.form-control,
    textarea.form-control,
    .gdInput,
    select.gdInput,
    input.gdInput {
        padding: 4px 8px !important;
        font-size: 12px !important;
        height: 28px !important;
        line-height: 1.4 !important;
    }

    textarea.form-control {
        height: auto !important;
        min-height: 60px !important;
    }

    select.form-control,
    select.FormclrcmbField {
        padding: 3px 8px !important;
    }

    /* Reduce spacing in form-label containers */
    .form-label {
        margin-bottom: 6px !important;
    }

    /* Reduce label font size */
    /*    .labelup {
        font-size: 12px !important;
    }*/

    /* Reduce table cell padding */
    table td {
        padding: 4px !important;
    }

    /* Reduce spacing for error divs */
    div[id^="divErr"] {
        padding-top: 2px !important;
        padding-left: 5px !important;
        font-size: 11px !important;
    }

    /* Compact row spacing */
    .row {
        margin-bottom: 8px !important;
    }

    /* Reduce button sizes slightly */
    .btn {
        padding: 5px 12px !important;
        font-size: 13px !important;
    }
</style>
<style>
    /* Align dropdown in DataTable cell */
    .action-dropdown {
        min-width: 100px;
        font-size: 12px;
        z-index: 1000;
        text-align: left;
    }

        /* Optional: Add hover effect */
        .action-dropdown > li > a {
            padding: 3px 12px;
            color: #333;
            text-decoration: none;
        }

            .action-dropdown > li > a:hover {
                background-color: #f5f5f5;
                color: #000;
            }

    /* Optional: Make dropdown button minimal */
    .dropdown-toggle {
        background-color: transparent !important;
        border: none !important;
        font-size: 15px;
        line-height: 1;
        padding: 2px 5px;
    }

        .dropdown-toggle::after {
            display: none;
        }
</style>
<body>

    @Html.HiddenFor(model => model.Add, new { @autocomplete = "off", @class = "form-control", @id = "txtAddID" })
    @Html.HiddenFor(model => model.Modify, new { @autocomplete = "off", @class = "form-control", @id = "txtModifyID" })
    @Html.HiddenFor(model => model.Return, new { @autocomplete = "off", @class = "form-control", @id = "txtReturnID" })
    @Html.HiddenFor(model => model.View, new { @autocomplete = "off", @class = "form-control", @id = "txtViewID" })
    @Html.HiddenFor(model => model.Cancel, new { @autocomplete = "off", @class = "form-control", @id = "txtCancelID" })
    @Html.HiddenFor(model => model.FormName, new { @autocomplete = "off", @class = "form-control", @id = "txtFormName" })
    @Html.HiddenFor(model => model.EnableReturnPrice, new { @autocomplete = "off", @class = "form-control", @id = "txtReurnPricePerm" })
    @Html.HiddenFor(model => model.EnableBranch, new { @autocomplete = "off", @class = "form-control", @id = "txtEnableBranch" })
    @Html.HiddenFor(model => model.ID, new { @autocomplete = "off", @class = "form-control FormclrField", @id = "txtID" })
    @Html.HiddenFor(model => model.TransType, new { @autocomplete = "off", @class = "form-control", @id = "txtTransType" })


    <div class="d-flex align-items-center gap-2">
        <!-- Larger heading -->
        <h4 class="mb-0" style="font-size:14px; padding-top:10px;" id="MainHeading">@ViewData["FormName"]</h4>
    </div>
    <div class="toastBox"></div>
    <div id="dvDocuments">
        <hr />
        <input hidden id="txtStatus" />
        <div class="w-100 mt-3">
            <div class="d-flex justify-content-end align-items-start mb-2 flex-wrap gap-2">

                <!-- Filter Controls -->
                <div class="d-flex align-items-start gap-2 flex-wrap">

                    <!-- Date Type -->
                    <div>
                        <select class="gdInput" id="cmbFilterDatetype" onchange="SetFilterDate();"></select>
                    </div>

                    <!-- From Date -->
                    <div>
                        <span class="form-label">
                            <input tabindex="1" class="form-control FormfilterclrField" type="date" id="dtpFFrom"
                                   oninput="if (this.value > this.max) { this.value = this.max; }">
                            <label class="labelup" for="dtpFFrom">From</label>
                        </span>
                        <div class="text-danger pt-1 ps-1 clrdivs" id="divErrFFrom"></div>
                    </div>

                    <!-- To Date -->
                    <div>
                        <span class="form-label">
                            <input tabindex="1" class="form-control FormfilterclrField" type="date" id="dtpFTo"
                                   oninput="if (this.value > this.max) { this.value = this.max; }">
                            <label class="labelup" for="dtpFTo">To</label>
                        </span>
                        <div class="text-danger pt-1 ps-1 clrdivs" id="divErrFTo"></div>
                    </div>

                    <!-- All Status Checkbox -->
                    <div class="d-flex align-items-center gap-1">
                        <input tabindex="1" class="form-check-input FormfilterclrField" type="checkbox" id="chkShowAll" style="font-size:14px;">
                        <label class="form-check-label" for="chkShowAll" style="font-size:14px;">All Status</label>
                    </div>

                    <!-- Filter Button -->
                    <div>
                        <button id="btnFilter" class="btn btn-primary">
                            <i class="bi bi-funnel" style="color:white;"></i>&#x2009;Filter
                        </button>
                    </div>

                </div>

            </div>

            <!-- Grid -->
            <div id="my-grid" style="height:600px; width:100%;"></div>

        </div>
    </div>
    <div id="dvMain">
        <hr />
        <!-- Row 1: Salesman, Branch, RefNo, DocID+Date -->
        <div class="row g-3 mb-3">

            <!-- Assign To -->
            <div class="col-12 col-md-2">
                <div class="input-group">
                    <span id="opensalesman" class="input-group-text" title="New Salesman"  style="height:28px !important"><span class="fas fa-arrow-up-right-from-square"></span></span>
                    <span class="form-label flex-fill">
                        <select class="form-control FormclrcmbField" id="cmbSalesman">
                            <option value="0">Select Salesman</option>
                        </select>
                        <label class="labelup" for="cmbSalesman">Assign To *</label>
                    </span>
                    <div class="text-danger pt-1 ps-1 clrdivs" id="divErrSalesman"></div>
                </div>
            </div>

            <!-- Branch -->
            <div class="col-12 col-md-2">
                <span class="form-label">
                    <select class="form-control FormclrcmbField" id="txtBranchName" @(Model.EnableBranch == "1" ? "disabled" : "")>
                        <option value="0">Select Branch</option>
                    </select>
                    <label class="labelup" for="txtBranchName">Branch *</label>
                </span>
                <div class="text-danger pt-1 ps-1 clrdivs" id="divErrBranch"></div>
            </div>

            <!-- Ref No -->
            <div class="col-12 col-md-2">
                <span class="form-label">
                    <input tabindex="1" class="form-control FormclrField" type="text" id="txtRefNo" placeholder=""
                           onbeforeinput="return AlphaNumericBeforeInput(event, this,'#divErrRefNo')"
                           oninput="sanitizeAlphaNumeric(this, '#divErrRefNo')" />
                    <label class="labelup" for="txtRefNo">Ref No</label>
                </span>
                <div class="text-danger pt-1 ps-1 clrdivs" id="divErrRefNo"></div>
            </div>

            <!-- Spacer -->
            <div class="col-12 col-md-3 col-lg-4"></div>

            <!-- Doc ID + Date (right side, stacked) -->
            <div class="col-12 col-md-3 col-lg-2 ms-auto" style="min-width: 200px;">
                <table>
                    <tr>
                        <td style="border:none;">
                            <span class="fw-semibold">Doc ID :</span>
                        </td>
                        <td style="border:none;">
                            <label id="lblDocID" docprefix="" class="fw-bolder" style="font-size:14px;">AI/2425/001</label>
                        </td>
                    </tr>
                    <tr>
                        <td style="border:none;">
                            <label style="font-size:14px">Date :</label>
                        </td>
                        <td style="border:none;">
                            <input class="clsdtpfieldbottomborder FormclrField" type="date" id="dtpDate"
                                   style="font-size:14px"
                                   oninput="if (this.value > this.max) { this.value = this.max; }" />
                        </td>
                    </tr>                  
                </table>
            </div>

        </div>

        <!-- Row 2: Filter Bar -->
        <hr />
        <div class="row g-3 align-items-start mt-1">
            <!-- Beat -->
            <div class="col-12 col-md-2 col-lg-2">
                <span class="form-label">
                    <select class="form-control FormclrcmbField" id="cmbFilterBeat">
                        <option value="0">Select Beat</option>
                    </select>
                    <label class="labelup" for="cmbFilterBeat">Beat</label>
                </span>
                <div class="text-danger pt-1 ps-1 clrdivs" id="divErrFilterBeat"></div>
            </div>

            <!-- Salesman -->
            <div class="col-12 col-md-2 col-lg-2">
                <span class="form-label">
                    <select class="form-control FormclrcmbField" id="cmbFilterSalesman">
                        <option value="0">Select Salesman</option>
                    </select>
                    <label class="labelup" for="cmbFilterSalesman">Salesman</label>
                </span>
                <div class="text-danger pt-1 ps-1 clrdivs" id="divErrFilterSalesman"></div>
            </div>

            <!-- Customer Name -->
            <div class="col-12 col-md-3 col-lg-3">
                <span class="form-label">
                    <input tabindex="1" class="form-control FormclrField" type="text" id="txtFilterCustomer" placeholder="">
                    <label class="labelup" for="txtFilterCustomer">Customer Name</label>
                </span>
                <div class="text-danger pt-1 ps-1 clrdivs" id="divErrFilterCustomer"></div>
            </div>

            <!-- As on Date -->
            <div class="col-12 col-md-2 col-lg-1">
                <span class="form-label">
                    <input tabindex="1" class="form-control FormclrField" type="date" id="dtpDocTo" placeholder=""
                           oninput="if (this.value > this.max) { this.value = this.max; }">
                    <label class="labelup" for="dtpDocTo">As on Date *</label>
                </span>
                <div class="text-danger pt-1 ps-1 clrdivs" id="divErrDocTo"></div>
            </div>

            <!-- Load Button -->
            <div class="col-12 col-md-2 col-lg-1 d-flex align-items-center">
                <button id="btnDocFilter" class="btn btn-primary w-100">
                    <i class="bi bi-download" style="color:white;"></i>&#x2009;Load
                </button>
            </div>

        </div>

        <!-- Row 3: Invoice Table -->
        <div class="row pb-3 mb-3">
            <div class="col-12 d-flex gap-2 mb-1">
                <button class="btn btn-info" id="btnAddInvoices">
                    <i class="bi bi-plus"></i> Add
                </button>
                <button class="btn btn-primary" id="btnViewInvoices">
                    <i class="bi bi-eye"></i> View
                </button>
            </div>
            <div class="col-12">
                <table class="table table-bordered row-border order-column" id="tblInvoiceData" width="100%">
                    <thead>
                        <tr>
                            <th class="text-center trclrforcolheader" style="font-size:12px; width:0%;">ID</th>
                            <th class="text-center trclrforcolheader" style="font-size:12px; width:10%;">
                                <input type="checkbox" class="form-check-input" id="chkSelectAllInvoice" />
                            </th>
                            <th class="text-center trclrforcolheader" style="font-size:12px; width:2%;">#</th>
                            <th class="text-center trclrforcolheader" style="font-size:12px; width:10%;">Doc ID</th>
                            <th class="text-center trclrforcolheader" style="font-size:12px; width:10%;">Doc Date</th>
                            <th class="text-center trclrforcolheader" style="font-size:12px; width:8%;">Ref No</th>
                            <th class="text-center trclrforcolheader" style="font-size:12px; width:9%;">Beat</th>
                            <th class="text-center trclrforcolheader" style="font-size:12px; width:8%;">Salesman</th>
                            <th class="text-center trclrforcolheader" style="font-size:12px; width:8%;">Customer</th>
                            <th class="text-center trclrforcolheader" style="font-size:12px; width:15%;">Net Amount</th>
                            <th class="text-center trclrforcolheader" style="font-size:12px; width:10%;">Balance</th>
                            <th class="text-center trclrforcolheader" style="font-size:12px; width:10%;">Ageing</th>
                            <th class="text-center trclrforcolheader" style="font-size:12px; width:10%;" title="Assigned Invoice Count">AIC</th>
                        </tr>
                    </thead>
                </table>
            </div>
        </div>

        <!-- Row 4: Remarks + Narration -->
        <div class="row pb-5 mb-5">
            <div class="col-12 col-md-3"></div>
            <div class="col-12 col-md-3"></div>
            <div class="col-12 col-md-3">
                <!-- Remarks -->
                <div class="mb-3">
                    <span class="form-label">
                        <input class="form-control FormclrField" type="text" id="txtRemarks" placeholder=""
                               onbeforeinput="return AlphaNumericBeforeInput(event, this,'#divErrRemarks')"
                               oninput="sanitizeAlphaNumeric(this, '#divErrRemarks')" />
                        <label class="labelup" for="txtRemarks">Remarks</label>
                    </span>
                    <div class="text-danger pt-1 ps-1 clrdivs" id="divErrRemarks"></div>
                </div>
            </div>
            <div class="col-12 col-md-3">                
                <!-- Narration -->
                <div class="mb-3">
                    <span class="form-label">
                        <textarea class="form-control FormclrField" rows="3" id="txtNarration"
                                  onbeforeinput="return AlphaNumericBeforeInput(event, this,'#divErrNarration')"
                                  oninput="sanitizeAlphaNumeric(this, '#divErrNarration')"></textarea>
                        <label class="labelupfortextarea" for="txtNarration">Narration</label>
                    </span>
                    <div class="text-danger pt-1 ps-1 clrdivs" id="divErrNarration"></div>
                </div>

            </div>
        </div>

        <!-- Footer: Print Profile, Copies, Action Buttons -->
        <div class="footerAction modal-footer d-flex align-items-start justify-content-between gap-3 flex-wrap p-2">

            <!-- Left: Print Profile + Copies -->
            <div class="d-flex align-items-start gap-2 flex-wrap">

                <!-- Print Profile -->
                <div>
                    <span class="form-label">
                        <select class="form-control" id="cmbPrintProfile" style="min-width:160px;">
                            <option value="0">Select</option>
                        </select>
                        <label class="labelup" for="cmbPrintProfile">Print Profile</label>
                    </span>
                    <div class="text-danger pt-1 ps-1 clrdivs" id="divErrPrintProfile"></div>
                </div>

                <!-- Copies -->
                <div>
                    <span class="form-label">
                        <input id="txtNoofCopies" class="form-control" placeholder="" value="1" style="min-width:90px;"
                               onbeforeinput="return NumericOnlyBeforeInput(event, this,'#divErrNoofCopies')"
                               oninput="sanitizeNumericOnly(this, '#divErrNoofCopies')" />
                        <label class="labelup" for="txtNoofCopies">Copies</label>
                    </span>
                    <div class="text-danger pt-1 ps-1 clrdivs" id="divErrNoofCopies"></div>
                </div>

            </div>

            <!-- Right: Action Buttons -->
            <div class="d-flex align-items-center gap-2 flex-wrap justify-content-end">
                <button type="button" title="Whatsapp" class="btn" id="btnWhatsapp" style="background-color:green; color:white;">
                    <i class="fa-brands fa-whatsapp"></i>
                </button>
                <button type="button" title="Mail" class="btn clrforpdf" id="btnEmail">
                    <i class="bi bi-envelope"></i>
                </button>
                <button type="button" title="PDF" class="btn clrforpdf" id="btnPDF">
                    <i class="fa-regular fa-file-pdf"></i>
                </button>
                <button type="button" class="btn  clrforpreview" id="btnPreview">
                    <i class="bi bi-printer"></i> Preview
                </button>
                <button type="button" class="btn clrforpreview" id="btnPrint">
                    <i class="bi bi-printer-fill"></i> Print
                </button>
                <button type="button" class="btn btn-edit-style" id="btnTEdit"><span class="glyphicon glyphicon-pencil"> Edit</span></button>
                <button type="button" class="btn btn-cancel-style" id="btnTCancel"><span class="glyphicon glyphicon-remove"> Cancel</span></button>
                <button type="button" class="btn btn-variant-style" id="btnTReturn"><span class="bi bi-arrow-return-left"> Return</span></button>
                <button type="button" class="btn btn-danger" id="btnClear">
                    <span class="glyphicon glyphicon-refresh"> Clear</span>
                </button>
                <button type="button" class="btn btn-success" id="btnSave">
                    <span class="glyphicon glyphicon-saved"> Save</span>
                </button>
                <button type="button" class="btn btn-warning" id="btnClose">
                    <i class="bi bi-x-circle"></i> Close
                </button>
            </div>

        </div>

    </div>
    <div class="modal fade" id="myModalfg" tabindex="-1" aria-labelledby="lblPopupheading" aria-hidden="true" data-bs-backdrop="static" data-bs-keyboard="false">
        <div class="modal-dialog modal-xl modal-dialog-scrollable">
            <div class="modal-content">

                <!-- Modal Header -->
                <div class="modal-header">
                    <h5 class="modal-title text-dark" id="lblPopupheading">Added Invoices</h5>
                    <button type="button" class="btn-close closefg" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>

                <!-- Modal Body -->
                <div class="modal-body">
                    <div id="dvAssign" class="w-100 pb-2">
                        <table class="table table-bordered row-border order-column w-100" id="tblInvoiceDataView">
                            <thead>
                                <tr>
                                    <th class="text-center trclrforcolheader" style="font-size:12px; width:0%;">ID</th>
                                    <th class="text-center trclrforcolheader" style="font-size:12px; width:10%;">
                                        <input type="checkbox" class="form-check-input" id="chkSelectAllInvoiceView" />
                                    </th>
                                    <th class="text-center trclrforcolheader" style="font-size:12px; width:2%;">#</th>
                                    <th class="text-center trclrforcolheader" style="font-size:12px; width:10%;">Doc ID</th>
                                    <th class="text-center trclrforcolheader" style="font-size:12px; width:10%;">Doc Date</th>
                                    <th class="text-center trclrforcolheader" style="font-size:12px; width:8%;">Ref No</th>
                                    <th class="text-center trclrforcolheader" style="font-size:12px; width:9%;">Beat</th>
                                    <th class="text-center trclrforcolheader" style="font-size:12px; width:8%;">Salesman</th>
                                    <th class="text-center trclrforcolheader" style="font-size:12px; width:8%;">Customer</th>
                                    <th class="text-center trclrforcolheader" style="font-size:12px; width:15%;">Net Amount</th>
                                    <th class="text-center trclrforcolheader" style="font-size:12px; width:10%;">Balance</th>
                                    <th class="text-center trclrforcolheader" style="font-size:12px; width:10%;">Ageing</th>
                                    <th class="text-center trclrforcolheader" style="font-size:12px; width:10%;" title="Assigned Invoice Count">AIC</th>
                                </tr>
                            </thead>
                        </table>
                    </div>
                </div>

                <!-- Modal Footer -->
                <div class="modal-footer" style="background-color:skyblue;">
                    <button type="button" class="btn btn-success" id="btnBatchAccept">
                        <i class="bi bi-floppy"></i> Accept
                    </button>
                    <button type="button" class="btn btn-warning" id="btnBatchClose" data-bs-dismiss="modal">
                        <i class="bi bi-x-circle"></i> Close
                    </button>
                </div>

            </div>
        </div>
    </div>
</body>


<script>
    //~Filter Action Buttons
    let mapusersRole = {
        canView: $("#txtViewID").val() == 1,
        canEdit: $("#txtModifyID").val() == 1,
        canVariant: $("#txtVariantID").val() == 1,
        canCancel: $("#txtCancelID").val() == 1,
        canReturn: $("#txtReturnID").val() == 1,
        canPrint: $("#txtViewID").val() == 1,
        canPreview: $("#txtViewID").val() == 1,
        canPDF: $("#txtViewID").val() == 1
    };
    var allGridActions = [], singleIconAction = [], menuActions = [], columns = [];
    var actions = {}, OtherActions = {};

    function LoadPermissions() {
        allGridActions = [
            {
                icon: '👁️',
                title: 'View Data',
                className: 'dg-action-view',
                displayType: 'single_icon',
                visible: mapusersRole.canView,
                handler: (rowdata) => {
                    $(".clrdivs").html("");
                    var ID = rowdata.ID;//[0];
                    nTransMode = 5;
                    $("#txtID").val(ID);
                    ViewActionButtonPermission();
                    GetData(3, ID);
                    $(".FormclrField,.FormclrcmbField").prop("disabled", true);
                    $("#btnSave,#btnClear,#btnDocFilter,#btnViewInvoices, #btnAddInvoices,#chkSelectAllInvoice").prop("disabled", true);
                    $("#btnSave,#btnClear,#btnDraft").prop("disabled", true);
                    $("#btnPreview,#btnPDF,#btnPrint").prop("disabled", false);
                    $("#btnPreview,#btnPDF,#btnPrint").show();
                    $("#btnSave,#btnClear,#btnDraft").hide();
                    $("#dvMain").show();
                    $("#dvDocuments").hide();
                    $("#btnEmail,#btnWhatsapp").show();
                    $("#MainHeading").text("View Assign Invoices");
                }
            },
            {
                icon: '✏️',
                title: 'Edit',
                className: 'dg-action-edit',
                visible: (row) => {
                    var isVisible = false;
                    if (row.Status !== 'Amended' && row.Status !== 'Cancelled' && row.Status !== 'Processed') {
                        if (mapusersRole.canEdit)
                            isVisible = true;
                    }
                    return isVisible;
                },
                displayType: 'menu_item',
                handler: (rowdata, gridInstance) => {
                    $(".clrdivs").html("");
                    var ID = rowdata.ID;//[0];
                    var sts = rowdata.Status;//[9];
                    if (sts != "Cancelled") {
                        $("#txtID").val(ID);
                        EditActionTrigger(rowdata);
                    }
                    else {
                        showErrorSnackbar("Cannot Modify.This document already cancelled");
                    }
                }
            },
            {
                icon: '↩️',
                title: 'Return',
                className: 'dg-action-return',
                visible: (row) => {
                    var isVisible = false;
                    var RTNYN = row.Status == 'Amended' || row.Status == 'Cancelled' || row.Status == 'Close' || row.Status == 'Processed' ? false : true;
                    if (mapusersRole.canReturn && RTNYN) {
                        isVisible = true;
                    }
                    return isVisible;
                },
                displayType: 'menu_item',
                handler: (rowdata, gridInstance) => {
                    $(".clrdivs").html("");
                    var ID = rowdata.ID;//[0];
                    var sts = rowdata.Status[9];
                    if (sts != "Cancelled") {
                        $("#txtID").val(ID);
                        ReturnActionTrigger(rowdata);
                    }
                    else {
                        showErrorSnackbar("This document already cancelled");
                    }
                }
            },
            {
                icon: '❌',
                title: 'Cancel',
                className: 'dg-action-edit',
                visible: (row) => {
                    var isVisible = false;
                    if (row.Status !== 'Amended' && row.Status !== 'Cancelled' && row.Status !== 'Processed') {
                        if (mapusersRole.canCancel)
                            isVisible = true;
                        else
                            isVisible = false;
                    }
                    return isVisible;
                },
                displayType: 'menu_item',
                handler: (rowdata, gridInstance) => {
                    $(".clrdivs").html("");
                    var ID = rowdata.ID;//[0];
                    var sts = rowdata.Status[9];
                    if (sts != "Cancelled") {
                        $("#txtID").val(ID);
                        CancelActionTrigger(rowdata);
                    }
                    else {
                        showErrorSnackbar("This document already cancelled");
                    }
                }
            },
            {
                icon: '🖨️',
                title: 'Print',
                className: 'dg-action-edit',
                visible: mapusersRole.canPrint,
                displayType: 'menu_item',
                handler: (rowData, gridInstance) => {
                    var ID = rowData.ID;//[0];
                    var sts = rowData.CurrentStatus;//[9];
                    $("#txtID").val(ID);
                    $("#txtStatus").val(sts);
                    //$("#btnPrint").click();
                    var getdef = TransactionPrintProfiles.filter(pr => pr.DefaultConfig == 1);
                    var defid = getdef.length > 0 ? getdef[0].ConfigID : 0;
                    var copies = getdef.length > 0 ? getdef[0].NoCopies : 1;
                    $("#txtNoofCopies").val(copies);
                    $('#cmbPrintProfile').val(defid);
                    PrintConfigID = defid;
                    setTimeout(() => {
                        $("#btnPrint").click();
                    }, 300);
                }
            },
            {
                icon: '🖨️',
                title: 'Preview',
                className: 'dg-action-edit',
                visible: mapusersRole.canPreview,
                displayType: 'menu_item',
                handler: (rowData, gridInstance) => {
                    var ID = rowData.ID;//[0];
                    var sts = rowData.CurrentStatus;//[9];
                    $("#txtID").val(ID);
                    $("#txtStatus").val(sts);
                    //$("#btnPreview").click();
                    var getdef = TransactionPrintProfiles.filter(pr => pr.DefaultConfig == 1);
                    var defid = getdef.length > 0 ? getdef[0].ConfigID : 0;
                    var copies = getdef.length > 0 ? getdef[0].NoCopies : 1;
                    $("#txtNoofCopies").val(copies);
                    $('#cmbPrintProfile').val(defid);
                    PrintConfigID = defid;
                    setTimeout(() => {
                        $("#btnPreview").click();
                    }, 300);
                }
            },
            {
                icon: "<i class='fa-regular fa-file-pdf' style='color:brown'></i>",
                title: '&ensp;PDF',
                className: 'dg-action-edit',
                visible: mapusersRole.canPDF,
                displayType: 'menu_item',
                handler: (rowData, gridInstance) => {
                    var ID = rowData.ID;//[0];
                    var sts = rowData.CurrentStatus;//[9];
                    $("#txtID").val(ID);
                    $("#txtStatus").val(sts);
                    //$("#btnPDF").click();
                    var getdef = TransactionPrintProfiles.filter(pr => pr.DefaultConfig == 1);
                    var defid = getdef.length > 0 ? getdef[0].ConfigID : 0;
                    var copies = getdef.length > 0 ? getdef[0].NoCopies : 1;
                    $("#txtNoofCopies").val(copies);
                    $('#cmbPrintProfile').val(defid);
                    PrintConfigID = defid;
                    setTimeout(() => {
                        $("#btnPDF").click();
                    }, 300);
                }
            },
            {
                icon: "<i class='fa-solid fa-envelope' style='color:brown'></i>",
                title: '&ensp;Mail',
                className: 'dg-action-edit',
                visible: true,
                displayType: 'menu_item',
                handler: (rowData, gridInstance) => {
                    var ID = rowData.ID;//[0];
                    var sts = rowData.CurrentStatus;//[9];
                    $("#txtID").val(ID);
                    $("#txtStatus").val(sts);
                    //$("#btnEmail").click();
                    var getdef = TransactionPrintProfiles.filter(pr => pr.DefaultConfig == 1);
                    var defid = getdef.length > 0 ? getdef[0].ConfigID : 0;
                    var copies = getdef.length > 0 ? getdef[0].NoCopies : 1;
                    $("#txtNoofCopies").val(copies);
                    $('#cmbPrintProfile').val(defid);
                    PrintConfigID = defid;
                    setTimeout(() => {
                        $("#btnEmail").click();
                    }, 300);
                }
            },
            {
                icon: "<i class='fa-brands fa-whatsapp' style='color:green'></i>",
                title: '&ensp;WhatsApp',
                className: 'dg-action-edit',
                visible: true,
                displayType: 'menu_item',
                handler: (rowData, gridInstance) => {
                    var ID = rowData.ID;//[0];
                    var sts = rowData.CurrentStatus;//[9];
                    $("#txtID").val(ID);
                    $("#txtStatus").val(sts);
                    $("#btniframeWhatsapp").click();
                }
            },
        ];
        singleIconAction = allGridActions.filter(a => a.displayType === 'single_icon');
        menuActions = allGridActions.filter(a => a.displayType === 'menu_item');
        actions = {
            "header": 'View',
            "type": "action",
            "width": 60,
            "align": "center",
            "visible": mapusersRole.canView,
            "specialColumn": true,
            "ShowinColumnOption": false,
            "actions": singleIconAction
        };
        OtherActions = {
            "header": "Action",
            "type": "action3dots",
            "width": 60,
            "align": "center",
            "visible": true,
            "specialColumn": true,
            "ShowinColumnOption": false,
            "actions": menuActions
        }
    }

    LoadPermissions();
    function ViewActionButtonPermission() {
        $("#btnTEdit,#btnTCancel,#btnTReturn").hide();
        if (mapusersRole.canEdit)
            $("#btnTEdit").show();
        if (mapusersRole.canCancel)
            $("#btnTCancel").show();
        if (mapusersRole.canReturn)
            $("#btnTReturn").show();
    }
</script>

<script type="text/javascript">
    $("#txtDocID").prop("disabled", true);
        $("#dvMain").hide();
    var InvoiceTable = $('#tblInvoiceData').DataTable({
        paging: false,
        searching: true,
        info: false,
        ordering: true,
        scrollY: "250px",
        scrollX: true,
        scrollCollapse: true,
        "columnDefs": [{ "targets": [0], "visible": false, searchable: false, filter: false, ordering: false }],
    });
    var InvoiceDataView = $('#tblInvoiceDataView').DataTable({
        paging: false,
        searching: true,
        info: false,
        ordering: true,
        scrollY: "250px",
        scrollX: true,
        scrollCollapse: true,
        "columnDefs": [{ "targets": [0], "visible": false, searchable: false, filter: false, ordering: false }],
    });

    var UID = '@Session["LoginUserID"]';
    var Aurl = '@Session["APIurl"]';
    LoadBranchByRoles('#txtBranchName');
    var DecVal = '@Session["DecimalValues"]';
    var RID = '@Session["RoleID"]';
    var nTransMode = 1, nIsDraft = 0;
    var PrintTransID = 10, PrintConfigID = 0;
    var grid, AssignInvoices;
    var itemorderby = '@Session["ItemOrderby"]';
var EnableALStartWith = '@Session["ALSearchstartwith"]' == "1";
var autolistfilterwith = EnableALStartWith;
var startwithfield = itemorderby == "1" ? "code" : itemorderby == "2" ? "name" : "search";
    GetColumnSettingsData(2, 30, 1,true);
    GetPrintProfile(PrintTransID);
    setDate("dtpDate", 0);
    setDate("dtpFFrom", 0);
    setDate("dtpFTo", 0);
    setDate("dtpDocFrom", 0);
    setDate("dtpDocTo", 0);
    setTimeout(function () { GetFilterData(); }, 800);

    GetData(1);

    $('input[type=number]').focusin(function (e) {
        $(this).select();
    });
    $('input[type=number]').click(function (e) {
        $(this).select();
    });
    var modal = new bootstrap.Modal(document.getElementById('myModalfg'));// document.getElementById("myModalfg");

    $('#btnBatchClose').click(function (e) {
        modal.hide();
    });

    var TempProd = [];
    var arrParty = [];
    var prodids = 0;
    var tpn = "";

    $('#dtpDate,#txtBranchName').on('focus', function () {
        $(this).data("oldvalue", $(this).val());
    });
    $("#dtpDate").change(function (e) {
        var DocDate = e.target.value;
        var id = e.target.id;
        var Branch = $("#txtBranchName").val();
        var prefix = document.getElementById("lblDocID").getAttribute("docprefix");
        var isedit = nTransMode == 2;
        GetDocumentID(DocDate, Branch, 21, prefix, isedit, id);
    });

    $("#txtBranchName").change(function (e) {
        if (typeof GetDocumentID !== "function") return;
        var DocDate = $("#dtpDate").val();
        var Branch = e.target.value;
        var id = e.target.id;
        var prefix = document.getElementById("lblDocID").getAttribute("docprefix");
        var isedit = nTransMode == 2;
        GetDocumentID(DocDate, Branch, 21, prefix, isedit, id);
    });
    function LoadSalesmandata(selID) {
        $.ajax({
            url: Aurl + "assigninvoices/getdata",
            type: 'get',
            contentType: 'json',
            data: {
                Mode: 1, ID: null, BeatID: null, SalesmanID: null, Party: null, FromDate: null,
                ToDate: null, Showall: null, FilterBranch: null
            },
            xhrFields: {
                withCredentials: true
            },
            success: function (data) {
                $('#cmbSalesman,#cmbFilterSalesman').find("option").remove();
                $('#cmbSalesman,#cmbFilterSalesman').append($('<option/>', {
                    value: 0,
                    text: "-- Select --",
                }));
                $.each(data, function (i, items) {
                    if (items.FType == 1) {
                        $('#cmbSalesman,#cmbFilterSalesman').append($('<option/>', {
                            value: items.ID,
                            text: items.Name,
                        }));
                    }
                });
                $('#cmbSalesman').val(selID);
            }
        });
    }
    //~Preload Data
    function GetData(nMode, strID = null, strBeatID = null, strSalesmanID = null, strParty = null, strFromDate = null,
        strToDate = null, strFilterBranch = null) {
        var strShowall = $("#chkShowAll").is(":checked") ? "1" : "0"
        $.ajax({
            url: Aurl + "assigninvoices/getdata",
            type: 'get',
            contentType: 'json',
            data: {
                Mode: nMode, ID: strID, BeatID: strBeatID, SalesmanID: strSalesmanID, Party: strParty, FromDate: strFromDate,
                ToDate: strToDate, Showall: strShowall, FilterBranch: strFilterBranch
},
            xhrFields: {
                withCredentials: true
            },
            success: function (data) {
                if (nMode == 1) {
                    $('#cmbSalesman,#cmbFilterBeat,#cmbFilterSalesman,#txtBranchName').find("option").remove();
                    $('#cmbSalesman,#cmbFilterBeat,#cmbFilterSalesman,#txtBranchName').append($('<option/>', {
                        value: 0,
                        text: "-- Select --",
                    }));
                    arrParty = [];
                    $.each(data, function (i, items) {
                        if (items.FType == 3) {
                            var cust = {
                                "label": items.Code + " | " + items.Name,
                                "name": items.Name,
                                "code": items.Code,
                                "id": items.ID,
                            }
                            arrParty.push(cust);
                        }
                        if (items.FType == 1) {
                            $('#cmbSalesman,#cmbFilterSalesman').append($('<option/>', {
                                value: items.ID,
                                text: items.Name,
                            }));
                        }
                        if (items.FType == 2) {
                            $('#cmbFilterBeat').append($('<option/>', {
                                value: items.ID,
                                text: items.Name,
                            }));
                        }
                        if (items.FType == 4) {
                            $("#lblDocID").text(items.Name);
                        }
                        if (items.FType == 5) {
                            $('#txtBranchName').append($('<option/>', {
                                value: items.ID,
                                text: items.Name,
                            }));
                        }
                    });
                    SetAutlistforFields();
                }
                else if (nMode == 2) {
                    AssignInvoices = data;

                    var currentSearchValue = ''; if (grid) { currentSearchValue = grid ? grid.state.searchTerm : ''; }
                    grid = new GKBSDynamicGrid('#my-grid', columns, AssignInvoices, {
                        enablePagination: true,
                        pageSize: ItemsperPage,
                        enableSearch: true,
                        stickyToWindow: true,
                        enableExport: false,
                        enablePrint: false,
                        enableColumnsBtn: false,
                        enableAddRow: false,
                        enableRemoveRow: false,
                        enableSorting: true,
                        searchTerm: currentSearchValue, singleIconAction: singleIconAction,
                        rowActionsMenu: menuActions,
                        customCellRenderer: (el, col, rowData) => {

                        },
                        customButtons: [
                            {
                                icon: '⚙️',
                                text: '',
                                title: 'Settings',
                                visible: true,
                                onClick: (gridInstance, event) => {
                                    modalGenColumnSettings.show();
                                    GendralColumnSettings(1, 30, 1, 0);
                                }
                            },
                            {
                                icon: '➕',
                                text: 'New',
                                title: 'New Assign Invoice',
                                visible: $("#txtAddID").val() == 1,
                                onClick: (gridInstance, event) => {
                                    nTransMode = 1;
                                    $("#dvMain").show();
                                    $("#dvDocuments").hide();
                                    $("#btnTEdit,#btnTCancel,#btnTReturn").hide();
                                    ClearForm();
                                    setTimeout(() => {
                                        GetData(5, 0, $("#cmbFilterBeat").val(), $("#cmbFilterSalesman").val(), $("#txtFilterCustomer").val(), null,
                                            $("#dtpDocTo").val());
                                        LoadBranchByRoles('#txtBranchName');
                                    }, 500);
                                    setTimeout(() => {
                                        var DocDate = $("#dtpDate").val();
                                        var Branch = $("#txtBranchName").val();
                                        GetDocumentID(DocDate, Branch, 21);
                                    }, 1000);
                                }
                            },
                        ],
                        onCellClick: (rowData, fieldName, cellValue, column) => {

                        },
                        onCellDoubleClick: (rowData, fieldName, cellValue, column) => {

                        },
                        onColumnResize: (colIndex, field, header, newWidth) => {
                            UpdateColumnWidth(30, 1, 0, colIndex, field, newWidth)
                        },
                    });
                    var custjsdata = data;
                    if (custjsdata.length==0) {
                        //if (ClickType == 1)
                            showErrorSnackbar("No records found");
                    }
                }
                else if (nMode == 3) {
                    //~Assign Data
                    var isDisable = nTransMode == 3 || nTransMode == 5 || nTransMode == 4 ? "disabled" : "";
                    $.each(data, function (i, items) {
                        $("#txtID").val(items.ID),
                            $("#dtpDate").val(items.Date),
                            $("#lblDocID").text(items.DocID).attr("docprefix", items.DocPrefix),
                            $("#cmbSalesman").val(items.SalesmanID),
                            $("#txtRefNo").val(items.RefNo),
                            $("#txtStatus").val(items.Status);
                        $("#txtRemarks").val(items.Remarks), $("#txtNarration").val(items.Narration);
                        $("#txtBranchName").val(items.BranchID)
                    });
                    $('#btnViewInvoices').hide();
                    var custjsdata = JSON.parse(data[0].lstJsonAssignDetails);
                    //console.log(custjsdata);
                    $('#tblInvoiceData').DataTable({
                        scrollY: "250px",
                        scrollX: true,
                        scrollCollapse: true,
                        paging: false,
                        destroy: true,
                        data: custjsdata,
                        fixedColumns: false,
                        ordering: true,
                        searching: true,
                        sorting: true,
                        columns: [
                            { "data": "ID", "autoWidth": true, "visible": false, className: "dt-body-left" },
                            {
                                className: "dt-body-center", sorting:false,
                                "mRender": function (data, type, value, meta) {
                                    var rowIndex = meta.row;
                                    var checkboxId = 'chkCustomerCheck_' + rowIndex;//value.CustomerID
                                    var _action = value.Status == "1" ? '<input type="checkbox" checked id="' + checkboxId + '"  name="CustomerCheck" ' + isDisable + ' style="" InvoiceID="' + value.ID + '" />' : '';
                                    return _action;
                                },
                            },
                            {
                                className: "dt-body-center",
                                "mRender": function (data, type, value, meta) {
                                    var rowIndex = (meta.row + 1);
                                    var _action = rowIndex;
                                    return _action;
                                },
                            },
                            //{ "data": "DocID", "autoWidth": true, className: "dt-body-left" },
                            {
                                className: "dt-body-center",
                                "mRender": function (data, type, value) {
                                    var _action = '<a href="javascript:void(0)" style="cursor: pointer;" class="clsInvoiceCollection">' + value.DocID + '</a>';
                                    return _action;
                                }
                            },
                            { "data": "Date", "autoWidth": true, className: "dt-body-left" },
                            { "data": "RefNo", "autoWidth": true, className: "dt-body-left" },
                            { "data": "Beat", "autoWidth": true, className: "dt-body-left" },
                            { "data": "Salesman", "autoWidth": true, className: "dt-body-left" },
                            { "data": "Customer", "autoWidth": true, className: "dt-body-left" },
                            { "data": "NetAmt", "autoWidth": true, className: "dt-body-right" },
                            { "data": "Balance", "autoWidth": true, className: "dt-body-right" },
                            { "data": "Ageing", "autoWidth": true, className: "dt-body-right" },
                            { "data": "AssignedInvoiceCount", "autoWidth": true, className: "dt-body-right" }
                        ],
                    });
                    InvoiceTable = $('#tblInvoiceData').DataTable();
                    if (InvoiceTable.rows().count() == 0) {
                        showErrorSnackbar("No records found")
                    }
                }
                else if (nMode == 5) {
                    var isDisable = nTransMode == 3 || nTransMode == 5 ? "disabled" : "";
                    var custjsdata = data;// JSON.parse();
                    $('#tblInvoiceData').DataTable({
                        scrollY: "250px",
                        scrollX: true,
                        scrollCollapse: true,
                        paging: false,
                        destroy: true,
                        data: custjsdata,
                        fixedColumns: false,
                        ordering: true,
                        searching: true,
                        sorting: true,
                        columns: [
                            { "data": "ID", "autoWidth": true, "visible": false, className: "dt-body-left" },
                            {
                                className: "dt-body-center", sorting: false,
                                "mRender": function (data, type, value, meta) {
                                    var rowIndex = meta.row;
                                    var checkboxId = 'chkCustomerCheck_' + rowIndex;//value.CustomerID
                                    var _action = '<input type="checkbox" id="' + checkboxId + '"  name="CustomerCheck" ' + isDisable + ' style="" InvoiceID="' + value.ID +'" />';
                                    return _action;
                                },
                            },
                            {
                                className: "dt-body-center",
                                "mRender": function (data, type, value, meta) {
                                    var rowIndex = (meta.row + 1);
                                    var _action = rowIndex;
                                    return _action;
                                },
                            },
                            //{ "data": "DocID", "autoWidth": true, className: "dt-body-left" },
                            {
                                className: "dt-body-left",
                                "mRender": function (data, type, value) {
                                    var _action = '<a href="javascript:void(0)" style="cursor: pointer;" class="clsInvoiceCollection">' + value.DocID + '</a>';
                                    return _action;
                                }
                            },
                            { "data": "Date", "autoWidth": true, className: "dt-body-left" },
                            { "data": "RefNo", "autoWidth": true, className: "dt-body-left" },
                            { "data": "Beat", "autoWidth": true, className: "dt-body-left" },
                            { "data": "Salesman", "autoWidth": true, className: "dt-body-left" },
                            { "data": "Customer", "autoWidth": true, className: "dt-body-left" },
                            { "data": "NetAmt", "autoWidth": true, className: "dt-body-right" },
                            { "data": "Balance", "autoWidth": true, className: "dt-body-right" },
                            { "data": "Ageing", "autoWidth": true, className: "dt-body-right" },
                            { "data": "AssignedInvoiceCount", "autoWidth": true, className: "dt-body-right" },
                        ],
                    });
                    InvoiceTable = $('#tblInvoiceData').DataTable();

                    if (InvoiceTable.rows().count() == 0) {
                        showErrorSnackbar("No records found")
                    }
                }
            }, error: function (data) {
                if (data.status == "401") {
                    var msg = data.responseJSON.Message;
                    ////showErrorSnackbar(msg + " Logout and Login again.");
                }
            }
        });
    }

    $('#btnTEdit').click(function (e) {
        var Did = $("#txtID").val();
        var Currsts = $("#txtStatus").val();
        $(".clrdivs").html("");
        var rowinfo = {
            ID: Did,
            CurrentStatus: $("#txtStatus").val()
        }
        $.ajax({
            url: Aurl + "validatedocument",
            type: 'get',
            contentType: 'json',
            data: { ActionType: 1, TransID: 21, ID: Did, Status: Currsts },
            xhrFields: {
                withCredentials: true
            },
            success: function (data) {
                $.each(data, function (i, items) {
                    if (items.MsgID == 0) {
                        EditActionTrigger(rowinfo);
                    }
                    else {
                        showErrorSnackbar(items.Message)
                    }
                });
            }
        });
    });
    function EditActionTrigger(rowData) {
        nTransMode = 2;
        GetData(3, rowData.ID);
        $(".FormclrField,.FormclrcmbField").prop("disabled", false);
        $("#btnSave,#btnClose,#btnDocFilter,#btnViewInvoices, #btnAddInvoices,#chkSelectAllInvoice").prop("disabled", false);
        $("#btnPreview,#btnPDF,#btnClear,#btnPrint").prop("disabled", true);
        $("#btnPreview,#btnPDF,#btnPrint,#btnClear,#btnDraft").hide();
        $("#btnTEdit,#btnTCancel,#btnTReturn").hide();
        $("#btnSave").show();
        $("#btnSave,#btnClose").prop("disabled", false);
        $("#dvMain").show();
        $("#dvDocuments").hide();
        $("#btnEmail,#btnWhatsapp").hide();
        $("#MainHeading").text("Modify Assign Invoices");
        if ($("#txtEnableBranch").val() == 1) {
            $("#txtBranchName").prop("disabled", true);
        }
    }
    $('#btnTCancel').click(function (e) {
        var Did = $("#txtID").val();
        var Currsts = $("#txtStatus").val();
        $(".clrdivs").html("");
        var rowinfo = {
            ID: Did,
            CurrentStatus: $("#txtStatus").val()
        }
        $.ajax({
            url: Aurl + "validatedocument",
            type: 'get',
            contentType: 'json',
            data: { ActionType: 2, TransID: 21, ID: Did, Status: Currsts },
            xhrFields: {
                withCredentials: true
            },
            success: function (data) {
                $.each(data, function (i, items) {
                    if (items.MsgID == 0) {
                        CancelActionTrigger(rowinfo);
                    }
                    else {
                        showErrorSnackbar(items.Message)
                    }
                });
            }
        });
    });
    function CancelActionTrigger(rowData) {
        $("#MainHeading").text("Cancel Assign Invoices")
        nTransMode = 4;
        GetData(3, rowData.ID);
        $(".FormclrField,.FormclrcmbField").prop("disabled", true);
        $("#txtNarration,#txtRemarks").prop("disabled", false);
        $("#btnSave,#btnClose").prop("disabled", false);
        $("#btnClear,#btnDocFilter,#btnViewInvoices, #btnAddInvoices,#chkSelectAllInvoice").prop("disabled", true);
        $("#dvMain").show();
        $("#btnSave").show();
        $("#dvDocuments").hide();
        $("#btnEmail,#btnWhatsapp,#btnPreview,#btnPDF,#btnPrint,#btnClear,#btnDraft").hide();
        $("#btnTEdit,#btnTCancel,#btnTReturn").hide();
    }
    $('#btnTReturn').click(function (e) {
        var Did = $("#txtID").val();
        var Currsts = $("#txtStatus").val();
        $(".clrdivs").html("");
        var rowinfo = {
            ID: Did,
            CurrentStatus: $("#txtStatus").val()
        }
        $.ajax({
            url: Aurl + "validatedocument",
            type: 'get',
            contentType: 'json',
            data: { ActionType: 2, TransID: 21, ID: Did, Status: Currsts },
            xhrFields: {
                withCredentials: true
            },
            success: function (data) {
                $.each(data, function (i, items) {
                    if (items.MsgID == 0) {
                        ReturnActionTrigger(rowinfo);
                    }
                    else {
                        showErrorSnackbar(items.Message)
                    }
                });
            }
        });
    });
    function ReturnActionTrigger(rowData) {
        $("#MainHeading").text("Return Assign Invoices")
        nTransMode = 6;
        GetData(3, rowData.ID);
        $(".FormclrField,.FormclrcmbField").prop("disabled", true);
        $("#btnSave,#btnClose").prop("disabled", false);
        $("#btnPreview,#btnPDF,#btnClear,#btnDraft,#btnPrint").prop("disabled", true);
        $("#btnPreview,#btnPDF,#btnPrint,#btnClear,#btnDraft").hide();
        $("#btnTEdit,#btnTCancel,#btnTReturn").hide();
        $("#btnEmail,#btnWhatsapp").hide();
        $("#btnSave").show();
        $("#dvMain").show();
        $("#dvDocuments").hide();
    }

    $(document).on("click", ".clsInvoiceCollection", function () {
    //$('#tblInvoiceData tbody').on('click', '.clsInvoiceCollection', function (e) {
        $(".clrdivs").html("");
        rowdata = InvoiceTable.row($(this).parents('tr')).data();
        var ID = rowdata.ID;//[0];
        InvoiceCollectionModalPopup(ID,15)
    });
    function SetAutlistforFields() {
        //~AutoList
        PaginatedAutocomplete({
            inputId: "txtFilterCustomer",
            data: arrParty,
            startwitfieldname: startwithfield,
            enablefilterstartwith: autolistfilterwith,
            onSelect: function (item) {
                $("#txtFilterCustomer").val(item.name);
                return false;
            }
        });
    }

    $('#btnDocFilter').click(function (e) {
        var IsValid = true;
        $(".clrdivs").html("");
        if ($("#txtFilterCustomer").val() != "") {
            const exists = arrParty.filter(cust => cust.name == $("#txtFilterCustomer").val().trim());
            if (exists.length == 0) {
                IsValid = false;
                $("#divErrFilterCustomer").html("Select Customer Name from list");
                showErrorSnackbar("Select Customer Name from list");
            }
        }
        if ($("#dtpDocTo").val() == "") {
            IsValid = false;
            $("#divErrDocTo").html("As on date should not be empty");
            showErrorSnackbar("As on date should not be empty");
        }
        if ($("#txtBranchName").val() == 0 || $("#txtBranchName").val() == "") {
            IsValid = false;
            $("#divErrFilterBranch").html("Select Branch Name");
            showErrorSnackbar("Select Branch Name");
        }
        if (IsValid) {
            //cmbSalesman,cmbFilterBeat,cmbFilterSalesman,txtFilterCustomer
            GetData(5, 0, $("#cmbFilterBeat").val(), $("#cmbFilterSalesman").val(), $("#txtFilterCustomer").val(), null,
                $("#dtpDocTo").val(), $("#txtBranchName").val());
        }
    });
    $('#btnFilter').click(function (e) {
        GetFilterData(1);
    });
    var Showall = '@Session["Showallstatus"]';
    $("#chkShowAll").prop("checked", Showall == '1');
    function GetFilterData(ClickType = 0) {//arrVendorName,divErrFTo,divErrFFrom,divErrFParty
        var IsValid = true;
        $(".clrdivs").html("");
        if ($("#dtpFFrom").val() =="") {
            IsValid = false;
            $("#divErrFFrom").html("From date should not be empty");
        }
        if ($("#dtpFTo").val() == "") {
            IsValid = false;
            $("#divErrFTo").html("To date should not be empty");
        }
        if ($("#dtpFFrom").val() > $("#dtpFTo").val()) {
            IsValid = false;
            $("#divErrFFrom").html("From date should be lessthan To Date");
        }
        if (IsValid) {
            GetData(2, 0, 0, 0, "", $("#dtpFFrom").val(),$("#dtpFTo").val());
        }
    }
    $(document).on("click", "#chkSelectAllInvoice", function () {
        var IsChk = $('#chkSelectAllInvoice').is(':checked');
        //alert(IsChk);
        $('#tblInvoiceData tr td input').each(function (i, items) {
            items.checked = IsChk;
        });
    });
    $(document).on("click", "#btnAddInvoices", function () {
        var chkanyone = false, IsValid = true, AlreadyExists = false;
        if (InvoiceTable.rows().count() > 0) {
            var anyChecked = $('#tblInvoiceData tbody input[type=checkbox]:checked').length > 0;
            if (!anyChecked) {
                showErrorSnackbar("Check anyone Invoice");
                IsValid = false;
            }
            if (IsValid) {
                var checkeditems = $('#tblInvoiceData tbody input[type=checkbox]:checked');
                checkeditems.each(function () {
                    var row = $(this).closest('tr'); // Find the parent row
                    var rowData = $('#tblInvoiceData').DataTable().row(row).data(); // Get full row data
                    CheckedInvoices.push(rowData);
                });
                const uniqueByID = Object.values(
                    CheckedInvoices.reduce((acc, item) => {
                        if (!acc[item.ID]) {
                            acc[item.ID] = item; // only take first occurrence
                        }
                        return acc;
                    }, {})
                );
                CheckedInvoices = uniqueByID
                $('#tblInvoiceData').empty();
                $('#btnViewInvoices').show();
                $('#txtBranchName').prop("disabled", true);

            }
        }
        else {
            showErrorSnackbar("No data found");
        }
    });

    $(document).on("click", "#btnViewInvoices", function () {
        modal.show();
        var custjsdata = CheckedInvoices;
        $('#tblInvoiceDataView').DataTable({
            scrollY: "250px",
            scrollX: true,
            scrollCollapse: true,
            paging: false,
            destroy: true,
            data: custjsdata,
            fixedColumns: false,
            ordering: true,
            searching: true,
            sorting: true,
            columns: [
                { "data": "ID", "autoWidth": true, "visible": false, className: "dt-body-left" },
                {
                    className: "dt-body-center", sorting: false,
                    "mRender": function (data, type, value, meta) {
                        var rowIndex = meta.row;
                        var checkboxId = 'chkCustomerCheck_' + rowIndex;//value.CustomerID
                        var _action = '<input type="checkbox" checked checked id="' + checkboxId + '"  name="CustomerCheck" style="" InvoiceID="' + value.ID + '" />';
                        return _action;
                    },
                },
                {
                    className: "dt-body-center",
                    "mRender": function (data, type, value, meta) {
                        var rowIndex = (meta.row + 1);
                        var _action = rowIndex;
                        return _action;
                    },
                },
                { "data": "DocID", "autoWidth": true, className: "dt-body-left" },
                { "data": "Date", "autoWidth": true, className: "dt-body-left" },
                { "data": "RefNo", "autoWidth": true, className: "dt-body-left" },
                { "data": "Beat", "autoWidth": true, className: "dt-body-left" },
                { "data": "Salesman", "autoWidth": true, className: "dt-body-left" },
                { "data": "Customer", "autoWidth": true, className: "dt-body-left" },
                { "data": "NetAmt", "autoWidth": true, className: "dt-body-right" },
                { "data": "Balance", "autoWidth": true, className: "dt-body-right" },
                { "data": "Ageing", "autoWidth": true, className: "dt-body-right" },
                { "data": "AssignedInvoiceCount", "autoWidth": true, className: "dt-body-right" },
            ],
        });
        InvoiceDataView = $('#tblInvoiceDataView').DataTable();
        $('#chkSelectAllInvoiceView').prop("checked", true);
    });
    $("#txtScanDocid").on("keydown", function (e) {
        if (e.key === "Enter") {
            e.preventDefault();
            var found = false;
            let value = this.value;//Doc ID
            //showSuccessSnackbar("Doc ID : " + value);
            var Notcheckeditems = $('#tblInvoiceData tbody input[type=checkbox]:not(:checked)');
            Notcheckeditems.each(function () {
                var row = $(this).closest('tr'); // Find the parent row
                var rowData = $('#tblInvoiceData').DataTable().row(row).data(); // Get full row data
                //CheckedInvoices.push(rowData);
                //console.log(" Row Doc ID : " + rowData.DocID);
                if (rowData && rowData.DocID === value) {
                    // ✅ Check the checkbox
                    $(this).prop('checked', true);

                    // Optional: add selected row style
                    row.addClass('selected');

                    found = true;
                    return false; // break loop
                }
            });
            if (!found) {
                showErrorSnackbar("Doc ID not found : " + value);
            }
            $("#txtScanDocid").focus();
            $("#txtScanDocid").select();
            $("#txtScanDocid").val("");
        }
    });
    $(document).on('change', 'input[type=checkbox]', function () {
        var row = $(this).closest('tr');
        if (this.checked) {
            row.addClass('selected');
        } else {
            row.removeClass('selected'); // ✅ remove when unchecked
        }
    });
    $(document).on("click", "#btnBatchAccept", function () {
        CheckedInvoices = [];
        var chkanyone = false, IsValid = true, AlreadyExists = false;
        if (InvoiceDataView.rows().count() > 0) {
            var anyChecked = $('#tblInvoiceDataView tbody input[type=checkbox]:checked').length > 0;
            if (!anyChecked) {
                showErrorSnackbar("Check anyone Invoice");
                IsValid = false;
            }
            if (IsValid) {
                //addSelectCustomersinArray();
                var checkeditems = $('#tblInvoiceDataView tbody input[type=checkbox]:checked');
                checkeditems.each(function () {
                    var row = $(this).closest('tr'); // Find the parent row
                    var rowData = $('#tblInvoiceDataView').DataTable().row(row).data(); // Get full row data
                    CheckedInvoices.push(rowData);
                });
                $('#tblInvoiceDataView').empty();
                $('#btnViewInvoices').show();
                modal.hide();
            }
        }
        else {
            showErrorSnackbar("No data found");
        }
    });
    //~Close
    $("#btnClose").click(function () {
        if (ClosecfFocus != 3) { CloseConfirmDialogMsg("Do you want to close?"); } else { CloseForm(); }
    });
    function CloseForm() {
        if ($("#txtTransType").val() == 3 || $("#txtTransType").val() == 2) {
            this.close();
        } else {
            $("#dvMain").hide();
            $("#dvDocuments").show();
            GetFilterData();
            $("#MainHeading").text("Assign Invoices");
        }
    }
    //~Clear
    $("#btnClear").click(function () {
        if (cfFocus != 3) { ClearConfirmDialogMsg("Do you want to clear?"); } else { ClearForm(); }
    });
    function ClearForm() {
        $(".FormclrField").val("");
        $(".FormclrcmbField").val(0);
        $(".clrdivs").html("");
        $(".FormclrField,.FormclrcmbField,#btnSave,#btnClear,#btnClose,#btnDocFilter,#btnViewInvoices, #btnAddInvoices,#chkSelectAllInvoice").prop("disabled", false);
        GetData(1);
        $("#btnEmail,#btnWhatsapp").hide();
        setDate("dtpDate", 0);
        setDate("dtpDocTo", 0);
        SetFilterDate();
        nIsDraft = 0;
        if (nTransMode != 1) {
            $("#dvMain").hide();
            $("#dvDocuments").show();
        }
        CheckedInvoices = [];
        $("#txtNoofCopies").val("1");
        InvoiceTable = $('#tblInvoiceData').DataTable();
        $('#chkSelectAllInvoice').prop("checked", false);
        $('#chkSelectAllInvoice').prop("disabled", false);
        $('#tblInvoiceData').empty();
        $('#btnViewInvoices').hide();
        $("#btnPreview,#btnPDF,#btnPrint").prop("disabled", true);
        $("#btnPreview,#btnPDF,#btnPrint").hide();
        $("#btnSave,#btnClear,#btnDraft").show();
        $("#MainHeading").text("Assign Invoices");
        $("#btnSave").prop("disabled", false);
        setTimeout(() => {
            if ($("#txtEnableBranch").val() == 1) {
                $("#txtBranchName").prop("disabled", true);
            }
            //$("#txtBranchName").val(DefBranch);
            LoadBranchByRoles('#txtBranchName');
        }, 500);
    }
    var CheckedInvoices = [];
    //~Validation
    function SaveValidation() {
        //CheckedInvoices = [];
        var IsValid = true;
        $(".clrdivs").html("");
        if ($("#cmbSalesman").val() == 0) {
            IsValid = false;
            $("#divErrSalesman").html("Select Salesman");
            showErrorSnackbar("Select Salesman");
        }
        if ($("#txtBranchName").val() == 0 || $("#txtBranchName").val() == null) {
            IsValid = false;
            $("#divErrBranch").html("Select Branch");
            showErrorSnackbar("Select Branch");
        }
        if ($("#dtpDate").val() == "") {
            IsValid = false;
            $("#divErrDate").html("Party should not be empty");
            showErrorSnackbar("Party should not be empty");
        }
        if (IsValid) {
            if (InvoiceTable.rows().count() > 0) {
                if (IsValid) {
                    //addSelectCustomersinArray();
                    var checkeditems = $('#tblInvoiceData tbody input[type=checkbox]:checked');
                    checkeditems.each(function () {
                        var row = $(this).closest('tr'); // Find the parent row
                        var rowData = $('#tblInvoiceData').DataTable().row(row).data(); // Get full row data
                        CheckedInvoices.push(rowData);
                    });
                    const uniqueByID = Object.values(
                        CheckedInvoices.reduce((acc, item) => {
                            if (!acc[item.ID]) {
                                acc[item.ID] = item; // only take first occurrence
                            }
                            return acc;
                        }, {})
                    );
                    CheckedInvoices = uniqueByID
                    if (CheckedInvoices.length == 0) {
                        IsValid = false;
                        showErrorSnackbar("Select atleast one Invoice");
                    } else {
                        $('#tblInvoiceData').empty();
                        $('#btnViewInvoices').show();
                    }
                }
            }
            else {
                IsValid = false;
                showErrorSnackbar("No data found");
            }
        }

        return IsValid;
    }
    function SaveData() {

            $("#btnSave").prop("disabled", true);
            //showErrorSnackbar("Validation success");
            var listTrans = {
                "ID": $("#txtID").val(),
                "Date": $("#dtpDate").val(),
                "TransMode": nTransMode,
                "TransID": 21,
                "DocID": $("#lblDocID").val(),
                "SalesmanID": $("#cmbSalesman").val(),
                "BranchID": $("#txtBranchName").val(),
                "RefNo": $("#txtRefNo").val(),
                "Remarks": $("#txtRemarks").val(),
                "Narration": $("#txtNarration").val(),
                "Status": nTransMode == 1 || nTransMode == 3 ? 1 : nTransMode == 2 ? 3 : $("#txtStatus").val(),
                "CurrentStatus": $("#txtStatus").val(),
                "CBy": UID,
                "UDFId" : 0,
                "lstAssignDetails": CheckedInvoices,
            }
            $.ajax({
                url: Aurl + "assigninvoices/save",
                type: 'POST',
                contentType: 'application/json; charset=utf-8',
                data: JSON.stringify(listTrans),
                cache: false,
                processData: false,
                xhrFields: {
                    withCredentials: true
                },
                success: function (data) {
                    $("#btnSave").prop("disabled", false);
                    $.each(data, function (i, items) {
                        if (items.MsgID == 0) {
                            showSuccessSnackbar(items.Message);
                            //ClearForm();
                            if (nTransMode != 4) {
                                if (allowprint == 1) {
                                    PrintDocument(items.ID, PrintTransID, PrintConfigID);
                                } else if (allowprint == 3 || allowprint == 4) {
                                    PrintConfirmDialogMsg(items.ID, PrintTransID, PrintConfigID, "Do you want to print ?");
                                }
                                //if (SendAutoMail == 1)
                                {
                                    //EmailDocument(items.ID, PrintTransID, PrintConfigID);
                                    AutoEmailDocument(items.ID, PrintTransID);
                                }
                            }
                            ClearForm();
                            GetFilterData();
                        }
                        else {
                            //$("#dvErrorName").html(items.Message);
                            showErrorSnackbar(items.Message);
                        }
                    });
                }, error: function (data) {
                    $("#btnSave").prop("disabled", false);
                    if (data.status == "401") {
                        var msg = data.responseJSON.Message;
                        ////showErrorSnackbar(msg + " Logout and Login again.");
                    }
                }
            });

    }
    $("#btnSave").click(function () {
        if (SaveValidation()) {
            if (cfFocus != 3) {
                ConfirmDialogMsg("Do you want to save data?");
            } else {
                SaveData();
            }
        }
    });
    //~View from another form(Home, Fin Rep Space click)
    if ($("#txtID").val() > 0 && $("#txtTransType").val() == 2) {
        $(".clrdivs").html("");
        nTransMode = 5;
        var ID = $("#txtID").val();
        GetData(3, ID);
        $(".FormclrField,.FormclrcmbField").prop("disabled", true);
        $("#btnSave,#btnClear,#btnDocFilter,#btnViewInvoices, #btnAddInvoices,#chkSelectAllInvoice").prop("disabled", true);
        //$("#btnClose").prop("disabled", false);
        $("#btnSave,#btnClear,#btnDraft").prop("disabled", true);
        $("#btnPreview,#btnPDF,#btnPrint").prop("disabled", false);
        $("#btnPreview,#btnPDF,#btnPrint").show();
        $("#btnSave,#btnClear,#btnDraft").hide();
        $("#dvMain").show();
        $("#dvDocuments").hide();
        $("#btnEmail,#btnWhatsapp").show();
        $("#MainHeading").text("View Assign Invoices");
    }
    var Url = '@Session["url"].ToString()';
    $("#opensalesman").click(function (e) {
        var nsmID = $("#cmbSalesman").val() != "" ? $("#cmbSalesman").val() : 0;
        vw = window.innerWidth;
        vh = window.innerHeight;
        const hasPermission = nsmID == 0
            ? checkAddEditPermission("79")
            : checkAddEditPermission("80");
        if (hasPermission) {
        var ProdUrl = Url + "SalesmanMaster?name=r8yIwuCefL0RbL6dbkzV7hS653HRxUkWTEY0xpdlCgc%3D&strFormID=EiUB5OIbEnvdq6f1lb1IEw%3D%3D&TypeID=BOII5FUynjpl5RZJJ8nW1g%3D%3D&TranID=jJOtHsRVwYECOhoiBc69dA%3D%3D&ID=" + nsmID;
            window.open(ProdUrl, "_blank", "width=" + vw + ",height=" + vh + ",scrollbars=yes,resizable=yes");
        }
        else {
            showErrorSnackbar("You don't have pemission for &ensp;<h4><code> " + (nsmID == 0 ? "Add" : "Edit") + " Salesman </code></h4>");
        }
    });
    document.addEventListener("keydown", function (event) {
        if (event.key === "F8") {
            event.preventDefault();
            var FieldID = event.target.id;
            var FieldName = event.target.name;
            if (FieldID == "cmbSalesman") {
                $("#opensalesman").trigger("click");
            }
        }
        else if (event.altKey && (event.key == 's')) {//Switch contains & startwith
            event.preventDefault();
            var fieldid = event.target.id;
            var infomessage = "Search mode changed to&ensp;" + (autolistfilterwith ? "<h4><code>Contains </code></h4>" : "<h4><code>Start with</code></h4>");
            autolistfilterwith = !autolistfilterwith;
            if (fieldid == "txtFilterCustomer") {
                showInfoSnackbar(infomessage);
                //~AutoList
                PaginatedAutocomplete({
                    inputId: "txtFilterCustomer",
                    data: arrParty,
                    startwitfieldname: startwithfield,
                    enablefilterstartwith: autolistfilterwith,
                    onSelect: function (item) {
                        $("#txtFilterCustomer").val(item.name);
                        return false;
                    }
                });
            }
        }
        else if (event.altKey && (event.key == 'a')) {//change search field
            event.preventDefault();
            var fieldid = event.target.id;
            startwithfield = (startwithfield == "code" ? "name" : startwithfield == "search" ? "code" : "search");
            if (fieldid == "txtFilterCustomer") {
                showInfoSnackbar("Search field changed to&ensp; <h4><code>'" + startwithfield + "'</code></h4>");
                //~AutoList
                PaginatedAutocomplete({
                    inputId: "txtFilterCustomer",
                    data: arrParty,
                    startwitfieldname: startwithfield,
                    enablefilterstartwith: autolistfilterwith,
                    onSelect: function (item) {
                        $("#txtFilterCustomer").val(item.name);
                        return false;
                    }
                });
            }
        }
        // autolistfilterwith = 1;
        else if (event.altKey && (event.key == 'q')) {//Info about Autolist property
            var containinfo = autolistfilterwith ? "Start With" : "Contains";
            var searchcolumninfo = startwithfield;

            var alinfo = `
<div class="p-3" style="min-width:360px;">  
    <!-- Search Mode -->
    <div class="border rounded-3 p-3 mb-2 bg-light">
        <div class="d-flex align-items-center justify-content-between">

            <div class="d-flex align-items-center">
                <div class="d-flex align-items-center justify-content-center rounded-circle bg-primary bg-opacity-10 text-primary me-2"
                     style="width:34px;height:34px;">
                    <i class="bi bi-search"></i>
                </div>

                <div>
                    <div class="text-muted medium fw-semibold">Search Mode</div>
                    <div class="fw-bold text-dark" style="font-size:16px">&emsp;<code>${containinfo}</code></div>
                </div>
            </div>

            <i class="bi bi-chevron-right text-muted"></i>
        </div>
    </div>

    <!-- Search Field -->
    <div class="border rounded-3 p-3 bg-light">
        <div class="d-flex align-items-center justify-content-between">

            <div class="d-flex align-items-center">
                <div class="d-flex align-items-center justify-content-center rounded-circle bg-success bg-opacity-10 text-success me-2"
                     style="width:34px;height:34px;">
                    <i class="bi bi-tag"></i>
                </div>

                <div>
                    <div class="text-muted medium fw-semibold">Search Field</div>
                    <div class="fw-bold text-dark" style="font-size:16px">&emsp;<code>${searchcolumninfo}</code></div>
                </div>
            </div>	

            <i class="bi bi-chevron-right text-muted"></i>
        </div>
    </div>

</div>
`;
            InfoDialogMsg(alinfo, "Autolist property");
        }


    });
    window.addEventListener("message", function (event) {
        var data = event.data;
        if (data && data.source === "SalesmanMaster") {
            var SMID = data.salesmanid;
            LoadSalesmandata(SMID);
        }

    });
</script>