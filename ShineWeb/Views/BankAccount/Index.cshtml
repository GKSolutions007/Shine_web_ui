@model ShineWeb.Models.SingleMasterModel
@{
    ViewBag.Title = ViewData["FormName"];
    if (ViewData["isDepend"].ToString() == "1")
    {
        Layout = null;
        //"~/Views/Shared/_LayoutWithoutheader.cshtml"

    }
    else
    {
        Layout = "~/Views/Shared/_LayoutBT5.cshtml";
    }
}
<link href="~/Content/5.3.8/bootstrap.min.css" rel="stylesheet" />
<link href="~/Content/navbar.css" rel="stylesheet" />
<link href="~/Content/buttonTop.css" rel="stylesheet" />
<link href="~/Content/dataTables.dataTables.2.2.2.css" rel="stylesheet" />
<link href="~/GKBS_Grid/css/gridui.css" rel="stylesheet" />

<script src="~/Scripts/5.3.8/bootstrap.min.js"></script>
<script src="~/Scripts/jquery-1.10.2.min.js"></script>
<script src="~/Scripts/jquery-3.7.1.js"></script>
<script src="~/Scripts/jquery-ui.js"></script>
<script src="~/Scripts/dataTables.2.2.2.js"></script>
<script src="~/Scripts/js/refreshtoken.js"></script>
<script src="~/GKBS_Grid/js/gridui.js"></script>

<style>  
    td {
        font-size: 11px;
    }

    input[type=number] {
        text-align: right;
    }

        input[type=number]::placeholder {
            text-align: left;
        }
    /* Chrome, Safari, Edge, Opera */
    input::-webkit-outer-spin-button,
    input::-webkit-inner-spin-button {
        -webkit-appearance: none;
        margin: 0;
    }

    /* Firefox */
    input[type=number] {
        -moz-appearance: textfield;
    }
    /* The Modal (background) */
    .modalBatch {
        display: none; /* Hidden by default */
        position: fixed; /* Stay in place */
        z-index: 200; /* Sit on top */
        left: 0;
        top: 0;
        width: 100%; /* Full width */
        height: 100%; /* Full height */
        overflow: auto; /* Enable scroll if needed */
        background-color: rgb(0,0,0); /* Fallback color */
        background-color: rgba(0,0,0,0.4); /* Black w/ opacity */
    }

    /* Modal Content/Box */
    @@media only screen and (min-width: 600px) {
        .modalBatch-content {
            background-color: #ffffff;
            margin: 3% auto; /* 15% from the top and centered */
            padding: 10px;
            border: 1px solid #888;
            width: 35%; /* Could be more or less, depending on screen size */
            height: 70%;
            top: 5%;
            overflow: auto;
        }
    }

    @@media only screen and (max-width: 600px) {
        .modalBatch-content {
            background-color: #ffffff;
            margin: 10% auto; /* 15% from the top and centered */
            padding: 25px;
            border: 1px solid #888;
            width: 95%; /* Could be more or less, depending on screen size */
            height: 80%;
            top: 20%;
            overflow: auto;
        }
    }
    /* The Close Button */
    .closefg {
        color: #aaa;
        float: right;
        font-size: 10px;
        font-weight: bold;
    }

        .closefg:hover,
        .closefg:focus {
            color: black;
            text-decoration: none;
            cursor: pointer;
        }

    .form-label {
        position: relative;
        display: block;
        clear: both;
        max-width: 500px;
        margin: 0 auto;
    }

    .labelup {
        -webkit-transform: translateY(50%);
        -moz-transform: translateY(-50%);
        -ms-transform: translateY(-50%);
        -o-transform: translateY(-50%);
        transform: translateY(50%);
        -webkit-transition: all 0.2s ease-in-out;
        -moz-transition: all 0.2s ease-in-out;
        transition: all 0.2s ease-in-out;
        position: absolute;
        top: -52%;
        left: 1em;
        background: #fffefe;
        color: #2b2828;
        font-size: 0.90em;
        cursor: text;
        pointer-events: none;
        font-family: sans-serif;
    }
</style>
<body>

    @Html.HiddenFor(model => model.Add, new { @autocomplete = "off", @class = "form-control", @id = "txtAddID" })
    @Html.HiddenFor(model => model.Modify, new { @autocomplete = "off", @class = "form-control", @id = "txtModifyID" })
    @Html.HiddenFor(model => model.View, new { @autocomplete = "off", @class = "form-control", @id = "txtViewID" })
    @Html.HiddenFor(model => model.FormName, new { @autocomplete = "off", @class = "form-control", @id = "txtFormName" })
    @Html.HiddenFor(model => model.ID, new { @autocomplete = "off", @class = "form-control FormclrField", @id = "txtID" })
    @Html.HiddenFor(model => model.ChildURL1, new { @autocomplete = "off", @class = "form-control", @id = "txtChildSMGrpURL" })

    <div class="d-flex align-items-center gap-2">
        <!-- Larger heading -->
        <h4 class="mb-0" style="font-size:14px; padding-top:10px;" id="MainHeading">@ViewData["FormName"]</h4>
    </div>
    <label id="lblIsDepend" hidden>@ViewData["isDepend"]</label>
    <label id="lblParentMenuID" hidden>@ViewData["ParentMenuID"]</label>
    <label id="lblParentFormFieldID" hidden>@ViewData["ParentFormFieldID"]</label>
    <hr />
    <div class="toastBox"></div>    
    <div id="dvMainTable">
        <div id="my-grid" style="height: 600px; width: 100%;"></div>
        <div hidden>
            <table class="tab   le table-bordered" id="tblMasterData" width="100%">
                <thead>                    
                    <tr class="trclrforcolheader">
                        <th style="text-align:center;font-size: 12px;width:0%;">ID</th>
                        <th style="text-align:center;font-size: 12px;width:0%;">BankID</th>
                        <th style="text-align:center;font-size: 12px;width:15%;">A/c No</th>
                        <th style="text-align:center;font-size: 12px;width:15%;">A/c Name</th>
                        <th style="text-align:center;font-size: 12px;width:10%;">Bank</th>
                        <th style="text-align:center;font-size: 12px;width:15%;">Branch</th>
                        <th style="text-align:center;font-size: 12px;width:10%;">IFSC</th>
                        <th style="text-align:center;font-size: 12px;width:10%;">MICR</th>
                        <th style="text-align:center;font-size: 12px;width:10%;">Set Default</th>
                        <th style="text-align:center;font-size: 12px;width:5%;">Active</th>
                        <th style="text-align:center;font-size: 12px;width:10%;">User</th>
                        <th style="text-align:center;font-size: 12px;width:5%;" title="Edit"><span class="glyphicon glyphicon-pencil"></span></th>
                        <th style="text-align:center;font-size: 12px;width:5%;" title="View"><span class="glyphicon glyphicon-eye-open"></span></th>
                    </tr>
                </thead>
            </table>
        </div>
    </div>
    <div class="modal fade" id="myModalfg" tabindex="-1" data-bs-backdrop="static" data-bs-keyboard="false">
        <div class="modal-dialog modal-sm">
            <div class="modal-content">

                <!-- Modal Header -->
                <div class="modal-header">
                    <h5 class="modal-title fw-bold" id="lblPopupheading">
                        Add @ViewData["FormName"]
                    </h5>
                    <button type="button" class="btn-close" id="modelcloseX"></button>
                </div>

                <!-- Modal body -->
                <div class="modal-body">
                    <div class="text-center pt-1">                        
                            <table class="table w-100">
                                <tr>
                                    <td style="border:none;padding-left:12px;padding-right:7px" colspan="2">
                                        <span class="form-label">
                                            <input tabindex="1" type="text" id="txtAccNo"
                                                   class="form-control FormclrField alphanumeric-only"
                                                   onbeforeinput="return AlphaNumericBeforeInput(event, this,'#dvErrorAccNo')"
                                                   oninput="sanitizeAlphaNumeric(this, '#dvErrorAccNo')" />
                                            <label class="labelup">Bank Account No *</label>
                                        </span>
                                        <div class="clrdiv text-danger" id="dvErrorAccNo"></div>
                                    </td>
                                </tr>

                                <tr>
                                    <td style="border:none;padding-left:12px;padding-right:7px" colspan="2">
                                        <span class="form-label">
                                            <input tabindex="2" type="text" id="txtAccName"
                                                   class="form-control FormclrField"
                                                   onbeforeinput="return AlphaNumericBeforeInput(event, this,'#dvErrorAccName')"
                                                   oninput="sanitizeAlphaNumeric(this, '#dvErrorAccName')" />
                                            <label class="labelup">Account Name *</label>
                                        </span>
                                        <div class="clrdiv text-danger" id="dvErrorAccName"></div>
                                    </td>
                                </tr>

                                <tr>
                                    <td style="border:none;padding-left:12px;padding-right:7px" colspan="2">
                                        <div class="input-group">
                                            <span id="openbankname" class="input-group-text" title="New Bank Name"><span class="fas fa-arrow-up-right-from-square"></span></span>
                                            <span class="form-label flex-fill">
                                                <select tabindex="3" id="cmbBank" class="form-control"></select>
                                                <label class="labelup">Bank Name *</label>
                                            </span>
                                            <div class="clrdiv text-danger" id="dvErrorBank"></div>
                                        </div>
                                    </td>
                                </tr>

                                <tr>
                                    <td style="border:none;padding-left:12px;padding-right:7px" colspan="2">
                                        <span class="form-label">
                                            <input tabindex="4" type="text" id="txtBranch"
                                                   class="form-control FormclrField alphanumeric-only" />
                                            <label class="labelup">Branch Name</label>
                                        </span>
                                        <div class="clrdiv text-danger" id="dvErrorBranch"></div>
                                    </td>
                                </tr>

                                <tr>
                                    <td style="border:none;padding-left:12px;padding-right:7px" width="50%">
                                        <span class="form-label">
                                            <input tabindex="5" type="text" id="txtIFSC"
                                                   class="form-control FormclrField alphanumeric-only"
                                                   maxlength="11" style="text-transform:uppercase" />
                                            <label class="labelup">IFSC Code</label>
                                        </span>
                                        <div class="clrdiv text-danger" id="dvErrorIFSC"></div>
                                    </td>

                                    <td style="border:none;padding-left:12px;padding-right:7px" width="50%">
                                        <span class="form-label">
                                            <input tabindex="6" type="number" id="txtMICR"
                                                   class="form-control FormclrField"
                                                   maxlength="9"
                                                   oninput="if (this.value.length > this.maxLength) { this.value = this.value.slice(0, this.maxLength); }" />
                                            <label class="labelup">MICR No</label>
                                        </span>
                                        <div class="clrdiv text-danger" id="dvErrorMICR"></div>
                                    </td>
                                </tr>

                                <tr>
                                    <td align="left" style="border:none">
                                        <label for="chkDefault">Set default :</label>
                                        <input tabindex="7" type="checkbox" id="chkDefault" />

                                        <div class="clrdiv text-danger" id="dvErrorDefault"></div>
                                    </td>

                                    <td  align="left" style="border:none">
                                        <label for="chkActive">Active :</label>
                                        <input tabindex="8" type="checkbox" id="chkActive" checked disabled />

                                        <div class="clrdiv text-danger" id="dvErrorActive"></div>
                                    </td>
                                </tr>

                            </table>
                    </div>
                </div>

                <!-- Modal footer -->
                <div class="modal-footer">
                    <div class="text-end w-100">
                        <button tabindex="10" type="button" class="btn btn-danger" id="btnClear">
                            <span class="glyphicon glyphicon-refresh"> Clear</span>
                        </button>
                        <button tabindex="9" type="button" class="btn btn-success" id="btnSave">
                            <span class="glyphicon glyphicon-saved"> Save</span>
                        </button>
                        <button tabindex="11" type="button" class="btn btn-warning" id="btnClose">
                            <i class="bi bi-x-circle"></i> Close
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>

<script>
    //~Filter Action Buttons
    const allGridActions = [
        {
            icon: '👁️',
            title: 'View Data',
            className: 'dg-action-view',
            // 💡 NEW TAG: This action will be rendered as a single icon
            displayType: 'single_icon',
            visible: $("#txtViewID").val() == 1,
            handler: (rowData) => {
                nMode = 3;
                $(".clrdiv").html("");
                //rowdata = Table.row($(this).parents('tr')).data();
                var ID = rowData.ID;//[0];
                var BankID = rowData.BankID;//[1];
                var AccNo = rowData.AccountNo;//[2];
                var AccName = rowData.AccountName;//[3];
                var Branch = rowData.BranchName;//[5];
                var IFSC = rowData.IFSCCode;//[6];
                var MICR = rowData.MICR;//[7];
                var SetDefault = rowData.SetDefault;//[8];
                var Active = rowData.Active;//[9];
                $(".modal-title#lblPopupheading").text("View " + $("#MainHeading").text());
                $("#txtID").val(ID);
                $("#txtAccNo").val(AccNo);
                $("#txtAccName").val(AccName);
                $("#cmbBank").val(BankID);
                $("#txtBranch").val(Branch);
                $("#txtIFSC").val(IFSC);
                $("#txtMICR").val(MICR);
                $("#chkActive").prop("checked", Active == "True");
                $("#chkDefault").prop("checked", SetDefault == "True");
                $(".FormclrField").prop("disabled", true);
                $("#chkActive,#btnSave,#btnClear,#cmbBank,#chkDefault").prop("disabled", true);
                $("#btnClear,#btnSave").hide();
                $("#btnClose").show();
                modal.show();
            }
        },
        {
            icon: '✏️',
            title: 'Edit Data',
            className: 'dg-action-edit',
            visible: $("#txtModifyID").val() == 1,
            displayType: 'single_icon',
            handler: (rowData, gridInstance) => {
                nMode = 2;
                $(".clrdiv").html("");
                //rowdata = Table.row($(this).parents('tr')).data();
                var ID = rowData.ID;//[0];
                var BankID = rowData.BankID;//[1];
                var AccNo = rowData.AccountNo;//[2];
                var AccName = rowData.AccountName;//[3];
                var Branch = rowData.BranchName;//[5];
                var IFSC = rowData.IFSCCode;//[6];
                var MICR = rowData.MICR;//[7];
                var SetDefault = rowData.SetDefault;//[8];
                var Active = rowData.Active;//[9];
                $(".modal-title#lblPopupheading").text("Edit " + $("#MainHeading").text());
                $("#txtID").val(ID);
                $("#txtAccNo").val(AccNo);
                $("#txtAccName").val(AccName);
                $("#cmbBank").val(BankID);
                $("#txtBranch").val(Branch);
                $("#txtIFSC").val(IFSC);
                $("#txtMICR").val(MICR);
                $("#chkActive").prop("checked", Active == "True");
                $("#chkDefault").prop("checked", SetDefault == "True");
                $(".FormclrField").prop("disabled", false);
                $("#chkActive,#btnSave,#cmbBank,#chkDefault").prop("disabled", false);
                $("#btnClear").prop("disabled", true);
                $("#btnClear").hide();
                $("#btnClose,#btnSave").show();
                modal.show();
            }
        }]
    var columns = [

    ];
    const singleIconAction = allGridActions.filter(a => a.displayType === 'single_icon');
    const menuActions = [];// allGrid3dotActions.filter(a => a.displayType === 'menu_item');
    var actions = {
        "field": "Action",
        "header": "Action",
        "type": "action",
        "width": 80,
        "align": "center",
        "visible": true,
        "specialColumn": true,
        "ShowinColumnOption": false,
        "actions": allGridActions
    };
</script>

<script type="text/javascript">
    document.addEventListener("DOMContentLoaded", function () {
        allowAlphanumericOnly('.alphanumeric-only');
    });
    var Aurl = '@Session["APIurl"]';

    var grid, BankAccount;
    GetColumnSettingsData(2, 9, 1);
    var UID = '@Session["LoginUserID"]';
    var nMode = 1;
    var FID = $("#txtFormID").val();
    GetData(3);
    GetData(5);
    var arrData = [];
    //~Grid Data
    function GetData(dMode, selectID) {
        $.ajax({
            url: Aurl + "bankaccount/get",
            type: 'get',
            contentType: 'json',
            data: { FormID: FID, Mode: dMode, Name: "" },
            xhrFields: {
                withCredentials: true
            },
            success: function (data) {
                    if (dMode == 3) {
                        if (data.length == 0) {
                            showErrorSnackbar("No Records Found");
                        }
                        BankAccount = data;
                        arrData = [];
                        $.each(data, function (i, items) {
                            //arrData[i] = items.AccountNo
                            var al = {
                                "label": items.AccountNo,
                                "id": items.ID,
                            }
                            arrData.push(al);
                        });

                        var currentSearchValue = ''; if (grid) { currentSearchValue = grid ? grid.state.searchTerm : ''; } grid = new GKBSDynamicGrid('#my-grid', columns, BankAccount, {
                            enablePagination: true,
                            pageSize: ItemsperPage,
                            enableSearch: true,
                            stickyToWindow: true,
                            enableExport: false,
                            enablePrint: false,
                            enableColumnsBtn: false,
                            enableAddRow: false,
                            enableRemoveRow: false,
                            enableSorting: true,
                            searchTerm: currentSearchValue, singleIconAction: singleIconAction, // Pass the single View action
                            rowActionsMenu: menuActions,       // Pass the Delete/Print actions
                            //onCellChange: handleCellUpdate,
                            customCellRenderer: (el, col, rowData) => {
                                if (col.field === 'Active') {
                                    const val = rowData[col.field];
                                    console.log("Active : ", val);
                                    if (val === 'True') {
                                        el.innerHTML = `<div class="dg-status-badge dg-status-active">
                      <span class="dg-status-circle"></span> Active
                  </div>`;
                                    } else if (val === 'False') {
                                        el.innerHTML = `<div class="dg-status-badge dg-status-inactive">
                      <span class="dg-status-circle"></span> In-Active
                  </div>`;
                                    }
                                }
                            },
                            customButtons: [
                                {
                                    icon: '⚙️',
                                    text: '',
                                    title: 'Settings',
                                    visible: true,
                                    onClick: (gridInstance, event) => {
                                        modalGenColumnSettings.show();
                                        GendralColumnSettings(1, 9, 1, 0);
                                    }
                                },
                                {
                                    icon: '➕',
                                    text: 'New',
                                    title: 'New Bank Account',
                                    visible: $("#txtAddID").val() == 1,
                                    onClick: (gridInstance, event) => {
                                        nMode = 1;
                                        $("#chkActive,#btnSave,#btnClear,.FormclrField,#cmbBank,#chkDefault").prop("disabled", false);
                                        $(".modal-title#lblPopupheading").text("Add " + $("#MainHeading").text());
                                        $("#btnClear,#btnClose,#btnSave").show();
                                        modal.show();
                                        ClearForm();
                                    }
                                },
                            ],
                            onCellClick: (rowData, fieldName, cellValue, column) => {

                            },
                            onCellDoubleClick: (rowData, fieldName, cellValue, column) => {

                            },
                            onColumnResize: (colIndex, field, header, newWidth) => {
                                UpdateColumnWidth(9, 1, 0, colIndex, field, newWidth)
                            },
                        });
                        //~AutoList
                        PaginatedAutocomplete({
                            inputId: "txtAccNo",
                            data: arrData,
                            onSelect: function (item) {
                                $("#txtAccNo").val(item.label);
                                return false;
                            }
                        });
                        //$("#txtAccNo").autocomplete({
                        //    minLength: 0,
                        //    source: arrData,
                        //    select: function (event, ui) {
                        //        $("#txtAccNo").val(ui.item.label);
                        //        return false;
                        //    },
                        //}).autocomplete("instance")._renderItem = function (ul, item) {
                        //    return $("<li class='each'>")
                        //        .append("<div class='acItem'><span class='name'>" +
                        //            item.label + "</span></div>")
                        //        .appendTo(ul);
                        //};
                    }
                    else {
                        $('#cmbBank').find("option").remove();
                        $('#cmbBank').append($('<option/>', {
                            value: 0,
                            text: "-- Select Bank --",
                            class: ""
                        }));
                        $.each(data, function (i, items) {
                            $('#cmbBank').append($('<option/>', {
                                value: items.ID,
                                text: items.Name,
                            }));
                        });
                        if (selectID) {
                            $('#cmbBank').val(selectID);
                        }
                    }
            }, error: function (data) {
                if (data.status == "401") {
                    var msg = data.responseJSON.Message;
                    ////showErrorSnackbar(msg + " Logout and Login again.");
                }
            }
        });
    }
    var modal = new bootstrap.Modal(document.getElementById('myModalfg'));//document.getElementById("myModalfg");
    $("#myModalfg").on("shown.bs.modal", function () {
        $("#txtAccNo").focus();
    })

    //~Close
    $('#btnClose,#modelcloseX').click(function (e) {
        if (ClosecfFocus != 3) { CloseConfirmDialogMsg("Do you want to close?"); } else { CloseForm(); }
    });
    function CloseForm() {
        modal.hide();
    }
    //~Clear
    $('#btnClear').click(function (e) {
        if (cfFocus != 3) { ClearConfirmDialogMsg("Do you want to clear?"); } else { ClearForm(); }
    });
    function ClearForm() {
        $(".FormclrField").val("");
        $("#cmbBank").val(0)
        $("#chkDefault").prop("checked", false);
        $("#chkActive").prop("checked", true);
        $("#chkActive").prop("disabled", nMode != 2);
        $(".clrdiv").html("");
        $("#txtAccNo").focus();
        if (nMode != 1)
            modal.hide();
    }
    //~Save
    $("#btnSave").click(function () {
        $(".clrdiv").html("");
        var IsValid = true, ChkFormation = true;
        //~Validation
        if ($("#txtAccNo").val() == "") {
            IsValid = false;
            $("#dvErrorAccNo").html("Account No should not be empty");
            showErrorSnackbar("Account No should not be empty");
        } else {
            ChkFormation = Alphaonly($("#txtAccNo").val(), $("#dvErrorAccNo"));
            if (!ChkFormation) {
                IsValid = false;
            }
        }
        if ($("#txtAccName").val() == "") {
            IsValid = false;
            $("#dvErrorAccName").html("Account Name should not be empty");
            showErrorSnackbar("Account Name should not be empty");
        }
        if ($("#cmbBank").val() == 0) {
            IsValid = false;
            $("#dvErrorBank").html("Select Bank Name");
            showErrorSnackbar("Select Bank Name");
        }
        ChkFormation = IFSCValidation($("#txtIFSC"), $("#dvErrorIFSC"));
        if (!ChkFormation) {
            IsValid = false;
        }
        ChkFormation = MICRValidation($("#txtMICR"), $("#dvErrorMICR"));
        if (!ChkFormation) {
            IsValid = false;
        }
        if (IsValid) {
            if (cfFocus != 3) { ConfirmDialogMsg("Do you want to save data?"); } else { SaveData(); }
        }
    });
    function SaveData() {
        $("#btnSave").prop("disabled", true);
        $(".clrdiv").html("");
        var lstMaster = {
            "Mode": nMode,
            "ID": $("#txtID").val(),
            "AccountNo": $.trim($("#txtAccNo").val()),
            "AccountName": $.trim($("#txtAccName").val()),
            "BankID": $("#cmbBank").val(),
            "BranchName": $("#txtBranch").val(),
            "IFSCCode": $("#txtIFSC").val().toUpperCase(),
            "MICR": $("#txtMICR").val(),
            "SetDefault": $("#chkDefault").is(":checked") ? 1 : 0,
            "Active": $("#chkActive").is(":checked") ? 1 : 0,
            "UserID": UID
        };
            $.ajax({
                url: Aurl + "bankaccount/save",
                type: 'POST',
                contentType: 'application/json; charset=utf-8',
                data: JSON.stringify(lstMaster),
                cache: false,
                processData: false,
                xhrFields: {
                    withCredentials: true
                },
                success: function (data) {
                    $("#btnSave").prop("disabled", false);
                    $.each(data, function (i, items) {
                        if (items.MsgID == 0) {
                            showSuccessSnackbar(items.Message);
                            ClearForm();
                            GetData(3);
                            modal.hide();
                            if ($("#lblIsDepend").text() == "1") {
                                var Par = $("#lblParentMenuID").text();
                                var fid = $("#lblParentFormFieldID").text();
                                parent.$("#myModalfgiframe").hide();
                                if (Par == "90")//from checkbook for bank acc
                                {
                                    parent.GetData(7);
                                    setTimeout(() => {
                                        parent.$("#" + fid).val(items.ID);
                                        parent.$("#" + fid).focus();
                                    }, 500)
                                }
                                //document.getElementById("myModalfgiframe").style.display = "none";
                            }
                        }
                        else {
                            $("#dvErrorAccNo").html(items.Message);
                            showErrorSnackbar(items.Message);
                        }
                    });
                }, error: function (data) {
                    $("#btnSave").prop("disabled", false);
                    if (data.status == "401") {
                        var msg = data.responseJSON.Message;
                        ////showErrorSnackbar(msg + " Logout and Login again.");
                    }
                }
            });
    }
    //window.addEventListener('keydown', function (event) {
    //    // Check if the F8 key is pressed (key code 119)
    //    if (event.keyCode === 119) {
    //        var url = $("#txtChildSMGrpURL").val();
    //        if (url != "") {
    //            var id = event.target.id;
    //            if (id == "cmbBank") {
    //                $("#lbliframeheading").text("Bank");
    //                document.getElementById("ifrSMG").src = url + "&ParFFID=cmbBank";
    //                // Prevent default action if any (e.g., browser F8 behavior)
    //                event.preventDefault();
    //                // Open the modal when F8 is pressed
    //                modaliframe.style.display = "block";
    //            }
    //        }
    //        else {
    //            showErrorSnackbar("Dependancy info not found");
    //        }
    //    }
    //});

            var Url = '@Session["url"].ToString()';
    $("#openbankname").click(function (e) {
        var nCustomerTypeID = $("#cmbBank").val() != "" ? $("#cmbBank").val() : 0;
        vw = window.innerWidth;
        vh = window.innerHeight;
        const hasPermission = nCustomerTypeID == 0
            ? checkAddEditPermission("3")
            : checkAddEditPermission("4");
        if (hasPermission) {
            var ProdUrl = Url + "SingleMaster?name=d1SRgPH3hrsAgtGtW9Kfmw%3D%3D&strFormID=mELirpUhRYksFj7k8%2FXBcQ%3D%3D&TypeID=BOII5FUynjpl5RZJJ8nW1g%3D%3D&TranID=jJOtHsRVwYECOhoiBc69dA%3D%3D&ID=" + nCustomerTypeID;
            window.open(ProdUrl, "_blank", "width=" + vw + ",height=" + vh + ",scrollbars=yes,resizable=yes");
        }
        else {
            showErrorSnackbar("You don't have pemission for &ensp;<h4><code>" + (nCustomerTypeID == 0 ? "Add" : "Edit") + " Bank</code></h4>");
        }
    });

    document.addEventListener("keydown", function (event) {
        if (event.key === "F8") {
            event.preventDefault();
            var FieldID = event.target.id;
            var FieldName = event.target.name;
            if (FieldID == "cmbBank") {
                $("#openbankname").trigger("click");
            }
        }
    });
    window.addEventListener("message", function (event) {
        var data = event.data;
        if (data && data.source === "Bank") {
            var CrTID = data.id;
            GetData(5, CrTID);
        }

    });
</script>